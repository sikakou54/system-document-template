<!-- コピーして 03_機能設計/03_API設計/エラーメッセージ一覧.md として使用。全 API のエラーコード(ERR-XXX)を一元管理する正本 -->
<!-- 各セクション直上のコメントに「定義内容(そのセクションの意味)」「定義する条件」「項目説明(各列・各項目の意味)」「定義ルール」をセットで記載する。編集時はコメントを読んでから該当セクションを埋める -->

<!--
【1. 概要】
定義内容: 本ファイルの役割(全 API のエラーコードを一元管理する正本であること)と、採番・区分・参照の方針を示す。
定義する条件: 必須。冒頭に 1〜3 行で記載する。
項目説明:
- 概要本文: 一元管理の対象(ERR-XXX)と、各 API 文書 §7・API-COM が本ファイルをエラーコードで参照する関係。
定義ルール:
- エラーコードはシステム全体を通した連番(共通・固有を区別せず 1 系列で採番)。採番は §2 一覧の最大値 +1、欠番の再利用は禁止、廃止は行を削除せず残し再利用しない。
- エラーレスポンスの封筒構造は API-COM_共通設計.md §4 が正本。発生条件は本ファイルに持たせず、共通エラー(区分=共通)は API-COM §7 共通処理フロー、固有エラー(区分=固有)は各 API 文書 §4/§5 個別処理フローで表現する。
-->
# 1. 概要

(全 API のエラーコード(ERR-XXX)を一元管理する正本である旨と、採番・区分・参照方針を 1〜3 行で記載)

<!--
【2. 一覧】
定義内容: 全 API のエラーコードを 1 行ずつ定義する。
定義する条件: 必須。エラーを追加・変更したら同一作業内で本表を更新する。
項目説明:
- エラーコード: このエラーの識別コード(ERR-XXX 連番)。返却時に API-COM §4 エラーレスポンスの封筒項目 error.code(論理名: エラーコード)に設定する。
- エラー名: エラーの日本語名称。
- 区分: 共通(全 API 共通処理フローで発生。API-COM §7)/ 固有(個別 API の処理フローで発生。各 API §4/§5)。
- HTTPステータス: このエラーで返す HTTP ステータスコード(400 / 401 / 403 / 404 / 409 / 500 など)。
- 開発者向けメッセージ: 返却時に API-COM §4 エラーレスポンスの封筒項目 error.message(論理名: 開発者向けメッセージ)に設定する開発者向けの説明文。実行時に埋め込む可変値は {0}, {1}(0始まり)のプレースホルダで表す。
- パラメータ: 開発者向けメッセージ中の {0}, {1}… に対応する値の説明(0始まり。例: {0}=予約ID)。可変値がない場合はブランク。
定義ルール:
- 採番は本表の最大値 +1。欠番の再利用は禁止。廃止は行を削除せず残し、再利用しない。
- 定義(エラー名・HTTPステータス・開発者向けメッセージ)は本表にのみ持ち、各 API 文書 §7・API-COM はエラーコードで参照する(再掲しない)。
- 発生条件(どの処理・判定で発生するか)は本表に持たせず、共通は API-COM §7、固有は各 API §4/§5 のフローで表現する。
- 開発者向けメッセージに実行時の可変値がある場合は {0}, {1}(0始まり)で表記し、パラメータ列に各プレースホルダの意味を記載する。可変値がない場合はパラメータ列はブランクとする。
-->
# 2. 一覧

| エラーコード | エラー名 | 区分 | HTTPステータス | 開発者向けメッセージ | パラメータ |
|---|---|---|---|---|---|
| ERR-001 |  | 共通 / 固有 | 400 / 401 / 403 / 404 / 409 / 500 |  |  |
