<!-- コピーして 03_機能設計/10_共通定義設計/DEF-XXX_名称.md として使用。index.md への行追加を先に行うこと -->
<!-- 共通定義設計は、複数文書から参照されるコード値・区分値・定数・共通ルールの正本を定義する。他文書では DEF-XXX/CODE-XXX 等のID参照のみを行い、値・表示名・説明を再定義しない -->

<!--
【1. 基本情報】
定義内容: この共通定義の識別情報と属性を一覧で示す。
定義する条件: 全共通定義で必須。
項目説明:
- 共通定義ID: この共通定義の識別子(DEF-XXX 連番)。
- 名称: 共通定義の日本語名称。
- 種別: 共通定義の分類(Code / Rule / Constant)。
- 概要: 定義する範囲・目的。
定義ルール:
- DEF-ID は DEF-XXX の連番。採番は一覧の最大値+1、欠番の再利用は禁止。
-->
# 1. 基本情報

| 項目 | 内容 |
|---|---|
| 共通定義ID | DEF-XXX |
| 名称 |  |
| 種別 | Code / Rule / Constant |
| 概要 |  |

<!--
【2. 定義ルール】
定義内容: この共通定義を参照・更新するときのルールを示す。
定義する条件: 全共通定義で必須。
定義ルール:
- 他文書から参照される値・表示名・説明は本書を正本とする。
- 他文書では DEF-XXX/CODE-XXX、DEF-XXX/SET-XXX 等のIDで参照し、値・表示名・説明を再記載しない。
- コード値の削除・再利用は禁止する。廃止する場合は状態を「廃止」として残す。
- 物理制約: テーブル設計で CHECK 制約など実装上必要な制約を記載してよいが、値の意味は本書を正本とする旨を示す。
-->
# 2. 定義ルール

| 項目 | 内容 |
|---|---|
| 正本 |  |
| 参照方法 |  |
| 値の扱い |  |
| 物理制約 |  |

<!--
【3. コード定義】
定義内容: 複数文書から参照されるコード値をコードIDごとに定義する。
定義する条件: 種別が Code の場合に定義する。該当しない場合は「なし」とする。
構成: コード種別ごとに ## CODE-XXX 名称 の見出しを作る。
項目説明:
- 値: 実装・データとして保持するコード値。
- 表示名: UI・設計書で表示する名称。
- 説明: コード値の意味。
- 状態: 有効 / 廃止。
定義ルール:
- CODE-ID は DEF 文書内で CODE-001 からの連番。他文書からは DEF-XXX/CODE-XXX で参照する。
- 値・表示名・説明は本セクションでのみ定義する。
-->
# 3. コード定義

## CODE-XXX 名称

| 値 | 表示名 | 説明 | 状態 |
|---|---|---|---|
|  |  |  | 有効 / 廃止 |

<!--
【4. 設定基準】
定義内容: コード値を設定・判定する場面ごとの基準を定義する。
定義する条件: 他文書で同一コードの初期値、成功時値、失敗時値、判定対象値を参照する場合に定義する。該当しない場合は「なし」とする。
項目説明:
- 基準ID: DEF 文書内の設定基準ID(SET-001 からの連番)。
- 対象コード: CODE-XXX 名称。
- 利用場面: 設定・判定が発生する業務場面。
- 設定・判定する状態: 本書のコード定義に存在する表示名。
定義ルール:
- SET-ID は DEF 文書内で SET-001 からの連番。他文書からは DEF-XXX/SET-XXX で参照する。
- 値と意味の対応は本セクションを正本とし、他文書では基準IDまたはコードIDを参照する。
-->
# 4. 設定基準

| 基準ID | 対象コード | 利用場面 | 設定・判定する状態 |
|---|---|---|---|
| SET-001 | CODE-XXX |  |  |
