<!-- コピーして 03_機能設計/09_運用設計/OPS-XXX_運用設計名.md として使用。index.md への行追加を先に行うこと -->
<!-- 作成・更新前に 11_トレーサビリティ/シーケンス設計トレーサビリティ.md で実現対象のNFR・関連SEQを特定して必ず読む。運用設計は Step 8。NFRの達成基準を監視・検知・バックアップ/復旧・デプロイ/切戻し・シークレット・JOB再処理・障害対応の運用手順として具体化する -->
<!-- 参照方向(§5): OPS→NFR、CMP/IFC、SEQ、Step 8の個別設計(運用対象)。上流のRQ/FR本文は参照しない。エラーはERRコードで参照(定義は エラーメッセージ一覧.md が正本) -->
<!-- 各見出し(##/###)直上のコメントに「定義内容(そのセクションの意味)」「定義する条件」「項目説明(各列・各項目の意味)」「定義ルール」をセットで記載する。編集時はコメントを読んでから該当セクションを埋める -->

<!--
【1. 基本情報】
定義内容: この運用設計の識別情報と属性(ID・運用名・分類・対象範囲・トレース元・概要・状態)を一覧で示す。
定義する条件: 全運用設計で必須。
項目説明:
- 運用ID: この運用設計の識別子(OPS-XXX 連番)。
- 運用名: 運用設計の日本語名称。
- 分類: 運用の種類(監視・アラート / バックアップ・リストア / デプロイ・切戻し / シークレット / JOB再処理 / 障害対応 等)。
- 対象範囲: この運用が適用される対象(システム全体 / 特定機能 / 特定JOB / 特定ストア 等)。
- トレース元: この運用設計が実現する NFR-ID。
- 概要: 運用設計の目的(1〜3行)。
- 状態: 未着手 / 作成中 / レビュー中 / 確定 / 廃止。
定義ルール:
- 運用ID は OPS-XXX の連番。採番は一覧の最大値+1、欠番の再利用は禁止。
- トレース元に対応する NFR-ID を記載する。運用の達成目標は当該 NFR の達成基準に整合させる。
- 分類は一覧の固有列「分類」と一致させる。
-->
# 1. 基本情報

| 項目 | 内容 |
|---|---|
| 運用ID | OPS-XXX |
| 運用名 |  |
| 分類 | 監視・アラート / バックアップ・リストア / デプロイ・切戻し / シークレット / JOB再処理 / 障害対応 |
| 対象範囲 | (システム全体 / 特定機能 / 特定JOB / 特定ストア など) |
| トレース元 | NFR-XXX |
| 概要 | (1〜3行) |
| 状態 | 未着手 / 作成中 / レビュー中 / 確定 / 廃止 |

<!--
【2. 関連設計】
定義内容: この運用設計が実現・監視・操作する対象の関連設計IDを、種別ごとに参照する。
定義する条件: 全運用設計で必須。関連する設計がある区分のみ記載し、該当が無い区分は「なし」とする。
項目説明:
- 関連NFR: この運用が実現する NFR-ID(達成基準の運用実現)。
- 関連CMP・IFC: 運用対象の構成要素・論理契約(01_概要設計)。
- 関連SEQ・JOB・API: 監視・再処理・復旧の対象となる連携・処理(SEQ / JOB / API)。
- 対象データ・ストア: バックアップ・復旧・監視の対象となるデータ・ストア(MDL / TBL 等、正本を参照)。
定義ルール:
- ID参照のみ記載し、参照先の仕様(NFRの達成基準・SEQの処理・TBLの構造など)は再記載しない。
- 網羅的な逆参照(この運用を利用する側の列挙)は禁止。運用の理解に必要な直接の対象IDだけを記載する。
- 参照できる先は §5 参照方向マトリクスの OPS 行(NFR、CMP/IFC、SEQ、Step 8の個別設計)に限る。上流のRQ/FR本文は参照しない。
-->
# 2. 関連設計

| 区分 | 関連ID |
|---|---|
| 関連NFR | NFR-XXX |
| 関連CMP・IFC | CMP-XXX / IFC-XXX |
| 関連SEQ・JOB・API | SEQ-XXX / JOB-XXX / API-XXX |
| 対象データ・ストア | MDL-XXX / TBL-XXX |

<!--
【3. 運用方針・手順】
定義内容: この運用の基本方針と実施手順(監視対象・実行タイミング・担当・使用ツール・実施ステップ)を定義する。
定義する条件: 全運用設計で必須。
項目説明:
- 方針: この運用で何を保証・実現するかの基本方針(箇条書き)。
- 手順: 実施する作業の順序・タイミング・担当・使用ツールを表で示す。
定義ルール:
- 方針は箇条書き(- )で1ポイント1項目に列挙する。単一ポイントは1〜3行の文でよい(§8-2)。
- 手順・タイミング・担当・ツールは表で書く(§8-2)。
- 運用対象の処理仕様(JOB・APIの内部処理)は再記載せず、対象IDを参照する。
- 実行基盤(Cloudflare/Stripe/Resend 等)の製品名・キュー名など運用対象の固有名は、運用上必要な範囲で最小限記載してよい(§8-13 例外)。
-->
# 3. 運用方針・手順

方針を箇条書きで、実施手順を表で定義する。

- (この運用で保証・実現する方針を1項目ずつ記載する)

| No | 手順 | タイミング | 担当 | 使用ツール |
|---|---|---|---|---|
| 1 |  |  |  |  |

<!--
【4. 検知・判定基準】
定義内容: 監視・判定の対象ごとに、正常/異常を切り分ける基準・閾値と、通知(発報)条件・通知先を定義する。
定義する条件: 監視・アラート・検知を伴う運用で必須。検知を伴わない運用では「なし」とする。
項目説明:
- 監視/判定対象: 監視・判定する指標や事象(応答時間、処理遅延、失敗回数、バックアップ成否 等)。
- 基準・閾値: 正常とみなす基準、異常と判定する閾値(数値・単位・測定条件)。
- 通知/発報条件: アラートを発報する条件(閾値超過・連続失敗回数・未完了時刻 等)。
- 通知先: 発報の宛先(運用者 / 管理者 / 担当窓口 等)。
定義ルール:
- 基準・閾値は、トレース元 NFR の達成基準(数値・単位・測定条件)を運用実現する値に整合させる(NFR本文の達成基準は再記載せず、運用で用いる判定値を定義する)。
- 通知先は役割(運用者・管理者)で記載し、個人名・具体的な連絡先は書かない。
- 監視ツール・通知チャネルの製品名は §3 で定義し、本表では判定基準に集中する。
-->
# 4. 検知・判定基準

| 監視/判定対象 | 基準・閾値 | 通知/発報条件 | 通知先 |
|---|---|---|---|
|  |  |  |  |

<!--
【5. 対応・復旧手順】
定義内容: 想定される事象(障害・失敗・異常)ごとに、一次対応・復旧手順・エスカレーション先を定義する。
定義する条件: 障害対応・復旧・再処理を伴う運用で必須。対応を伴わない運用では「なし」とする。
項目説明:
- 事象: 発生しうる障害・失敗・異常の事象(処理停止、外部連携失敗、バックアップ失敗 等)。エラーコードで特定できる場合は ERR-XXX を併記する。
- 一次対応: 発生直後に行う応急対応(切り離し・停止・再試行抑止 等)。
- 復旧手順: 正常状態へ戻す手順(再処理・リストア・切戻し 等)。関連するJOB再処理・デプロイ切戻しは対象IDを参照する。
- エスカレーション: 一次対応で解決しない場合の引き継ぎ先(役割)。
定義ルール:
- 事象にエラーが対応する場合は ERR-XXX で参照する(エラー名・HTTPステータス・文言の定義は エラーメッセージ一覧.md が正本のため再記載しない)。
- 復旧手順は、対象JOB・APIの処理内容を再記載せず、実行する処理を対象IDと操作(再実行・切戻し等)で示す。
- エスカレーションは役割(運用者・管理者・設計責任者)で記載する。
-->
# 5. 対応・復旧手順

| 事象 | 一次対応 | 復旧手順 | エスカレーション |
|---|---|---|---|
|  |  |  |  |

<!--
【6. 制限事項・前提】
定義内容: この運用が成立する前提条件と、適用上の制限事項を分けて定義する。
定義する条件: 全運用設計で必須。該当が無い区分は「なし」とする。
項目説明:
- 前提条件: この運用が成立するために必要な条件(実行基盤・権限・データ保持期間・稼働時間帯 等)。
- 制限事項: 運用上できないこと・対象外・制約(復旧不可範囲・手動対応が必要な範囲 等)。
定義ルール:
- 「制約・補足」などの自由記述欄にまとめず、前提条件と制限事項に分けて記載する(§8-8)。
- 各項目は箇条書き(- )で1項目ずつ列挙する。
-->
# 6. 制限事項・前提

## 前提条件

- (この運用が成立するために必要な条件を1項目ずつ記載する)

## 制限事項

- (運用上できないこと・対象外・制約を1項目ずつ記載する)

<!--
【7. 状態】
定義内容: この運用設計の状態を示す。
定義する条件: 全運用設計で必須。
項目説明:
- 状態値: 未着手 / 作成中 / レビュー中 / 確定 / 廃止 のいずれか。
定義ルール:
- 状態値のみを記載する(定義・遷移条件は CLAUDE.md §7 が正本)。
- §1 基本情報の状態および index.md 一覧の状態と一致させる。
-->
# 7. 状態

未着手 / 作成中 / レビュー中 / 確定 / 廃止
