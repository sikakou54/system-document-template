<!-- コピーして 03_機能設計/08_クエリ設計/SQL-XXX_クエリ名.md として使用。03_機能設計/08_クエリ設計/index.md への行追加を先に行うこと -->
<!-- 作成・更新前に 11_トレーサビリティ/traceability_sequence_design.md で関連SEQを特定して必ず読む。SEQのデータ参照・更新からクエリ目的・入力・出力・対象MDL/TBLを抽出する。SQLの実行元はMODだけとする -->
<!-- SQL は採用する RDB の方言で記載する。日時の保存形式・比較方法はプロジェクトの日時規約に従う -->
<!-- テーブル・カラムの物理名・型の正本は 03_機能設計/07_データベース設計(TBL-XXX)、共通コード・区分値の正本は 03_機能設計/10_共通定義設計(共通コード定義)。本文書はカラム定義や共通コード値を再記載せずIDで参照する -->
<!-- 各見出し(##/###)直上のコメントに「定義内容(そのセクションの意味)」「定義する条件」「項目説明(各列・各項目の意味)」「定義ルール」をセットで記載する。編集時はコメントを読んでから該当セクションを埋める -->

<!--
【1. 基本情報】
定義内容: このクエリの識別情報と属性(ID・名称・概要・用途・使用元)を一覧で示す。
定義する条件: 全クエリで必須。
項目説明:
- クエリID: このクエリの識別子(SQL-XXX 連番)。
- クエリ名: クエリの日本語名称(論理名)。
- 概要: クエリの目的(1〜3行)。
- 用途: このクエリで実現する業務・機能(1行)。
- 使用元: このクエリを呼び出すモジュール(MOD-XXX)。
定義ルール:
- クエリIDは SQL-XXX の連番。採番は一覧の最大値+1、欠番の再利用は禁止。
- 使用元は MOD-ID で参照する。API・JOBからSQLを直接実行してはならない。
-->
# 1. 基本情報

| 項目 | 内容 |
|---|---|
| クエリID | SQL-XXX |
| クエリ名 |  |
| 概要 | (1〜3行) |
| 用途 |  |
| 使用元 | MOD-XXX |

<!--
【2. 対象テーブル】
定義内容: このクエリが参照するテーブルと、その参照目的を示す。
定義する条件: 全クエリで必須。参照するテーブルを1件以上定義する。
項目説明:
- テーブル: 参照するテーブル(TBL-XXX。正本は データベース設計)。
- 物理名: テーブルの物理名(SQL内で使用する名称)。
- 用途: このクエリでの参照目的(抽出対象 / 結合 / 集計 など)。
定義ルール:
- テーブルは TBL-ID で参照する(カラム定義は再記載しない)。
-->
# 2. 対象テーブル

| テーブル | 物理名 | 用途 |
|---|---|---|
| TBL-XXX |  |  |

<!--
【3. 入力パラメータ】
定義内容: このクエリが受け取るバインドパラメータを、型・必須・制約とともに定義する。
定義する条件: バインドパラメータを持つクエリで定義する。無ければ「なし」の行を残す。
項目説明:
- パラメータ: SQL内のバインド変数名(:name 形式)。
- 論理名: パラメータの日本語表示名。
- 型: 値の型(INTEGER / TEXT / NUMERIC など、採用RDBの型)。
- 必須: 指定が必須か(Yes / No)。
- 説明・制約: 値の意味・形式・範囲・既定値など。
定義ルール:
- 日時・月の受け取り形式はプロジェクトの日時規約に従う(例: 月は 'YYYY-MM' 形式)。
- 可変長リスト(IN 句)はプレースホルダをアプリ側で展開する旨を制約に記載する。
-->
# 3. 入力パラメータ

| パラメータ | 論理名 | 型 | 必須 | 説明・制約 |
|---|---|---|---|---|
| :xxx |  |  | Yes |  |

<!--
【4. クエリ】
定義内容: 実行する SQL(採用RDBの方言)を記載する。
定義する条件: 全クエリで必須。
定義ルール:
- 採用RDBの方言で記載する(日付・時刻関数等はRDBの仕様に従う)。
- 論理削除は DELETED_AT IS NULL で有効行に限定する(共通カラム.md)。
- 区分値・ステータスは 03_機能設計/10_共通定義設計 の 共通コード定義/CODE-XXX を参照し、値の意味を再定義しない。
- バインド変数は §3 のパラメータ名(:name)を用いる。
-->
# 4. クエリ

```sql
SELECT ...
FROM ...
WHERE ...;
```

<!--
【5. 出力(列)】
定義内容: このクエリが返す結果列を、論理名・型・説明で定義する。
定義する条件: 全クエリで必須。返す列を1件以上定義する。
項目説明:
- 列名: SELECT が返す列の別名(実装キー)。
- 論理名: 列の日本語表示名。
- 型: 値の型(INTEGER / TEXT など)。
- 説明: 列の意味・算出方法。
定義ルール:
- 集計列は算出方法(COUNT / SUM / CAST 式など)を説明に記載する。
-->
# 5. 出力(列)

| 列名 | 論理名 | 型 | 説明 |
|---|---|---|---|
|  |  |  |  |

<!--
【6. 補足(性能・インデックス)】
定義内容: このクエリの性能上の考慮点(利用インデックス・想定件数・注意点)を示す。
定義する条件: 全クエリで定義する。該当が無い項目は「なし」とする。
項目説明:
- 項目: 補足の観点(利用インデックス / 想定件数 / 注意点 など)。
- 内容: 具体的な内容(利用する TBL-XXX/IDX-x、性能上の留意点など)。
定義ルール:
- 利用インデックスは TBL-XXX/IDX-x で参照する。
-->
# 6. 補足(性能・インデックス)

| 項目 | 内容 |
|---|---|
| 利用インデックス | TBL-XXX/IDX-x |
| 想定件数 |  |
| 注意点 |  |
