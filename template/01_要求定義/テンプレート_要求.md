<!-- 補足説明が必要な要求のみ、コピーして 01_要求定義/RQ-XXX_要求名.md として使用。要求一覧.md への行追加を先に行うこと -->
<!-- 要求(RQ)は、利用者・管理者・組織が対象システムに求めることを定義する。後続工程のIDは参照せず、why / want を扱う -->
<!-- 各セクション直上のコメントに「定義内容(そのセクションの意味)」「定義する条件」「項目説明(各列・各項目の意味)」「定義ルール」をセットで記載する。編集時はコメントを読んでから該当セクションを埋める -->

<!--
【1. 基本情報】
定義内容: この要求の識別情報(要求ID・要求名・要求主体・優先度・概要・背景)を一覧で示す。
定義する条件: 全 RQ で必須。
項目説明:
- 要求ID: この要求の識別子(RQ-XXX 連番)。
- 要求名: 要求の日本語名称。
- 要求主体: この要求を持つ利用者・部門・組織(ユーザーストーリーの「誰」)。
- 優先度: MoSCoW 区分(M/S/C/W)。要求一覧.md の要求一覧と同じ値を転記する。
- 概要: この要求の内容(1〜3行)。
- 背景: この要求が必要とされる理由・現状の課題。
定義ルール:
- 要求ID は RQ-XXX の連番。採番は 要求一覧.md の最大値 +1(欠番の再利用は禁止)。
- 優先度は 要求一覧.md の要求一覧を正本とし、本ファイルは同じ値を転記する(二重管理しない)。
- 利用者・管理者・組織が求める状態を書く。画面項目・API仕様・DB項目・処理ロジック・実装方式(技術名)は書かない。
- 概要・背景は簡潔に(各1〜3行)。
-->
# 1. 基本情報

| 項目 | 内容 |
|---|---|
| 要求ID | RQ-XXX |
| 要求名 |  |
| 要求主体 |  |
| 優先度 |  |
| 概要 |  |
| 背景 |  |

<!--
【2. 要求内容】
定義内容: 要求主体が対象システムに求めることを、目的(なぜ)と手段(何がほしい)に分けて定義する。
定義する条件: 全 RQ で必須。
項目説明:
- No: 項目の連番。
- 目的(なぜ): 要求主体が達成したいこと・解決したい課題。
- 手段(何がほしい): 目的を達成するために求める機能・状態。
定義ルール:
- 要求レベルの why(目的)と what(手段)のみを書く。処理ロジック・実装方式は書かない。
- 目的(なぜ)と手段(何がほしい)を必ず分けて記載する。手段だけ・目的だけの記載は禁止。
- 1行1件で、充足可否を判断できる粒度で記載する。
-->
# 2. 要求内容

| No | 目的(なぜ) | 手段(何がほしい) |
|---|---|---|
| 1 |  |  |

<!--
【3. 期待成果】
定義内容: この要求が満たされたときに得られる成果を定義する。
定義する条件: 全 RQ で必須。
項目説明:
- No: 項目の連番。
- 内容: 要求達成により得られる業務上・組織上の効果。
定義ルール:
- 成果は要求主体の観点で記載する。
- 画面・API・DBなどの設計詳細は書かない。
-->
# 3. 期待成果

| No | 内容 |
|---|---|
| 1 |  |

<!--
【4. 前提・制約】
定義内容: この要求が成り立つための前提と、要求に設ける制約・範囲外を定義する。
定義する条件: 前提・制約がある場合に定義する。無ければ No 列に「-」、内容列に「なし」を記載する。
項目説明:
- No: 項目の連番(無い場合は「-」)。
- 区分: 前提／制約／未確認。
- 内容: 成立前提、制約・範囲外、または未確認事項。
定義ルール:
- 実装方式・技術名は書かない(要求レベルの前提・制約のみ)。
- 未確認事項は要求本文に混ぜず、本セクションに分離する。
-->
# 4. 前提・制約

| No | 区分 | 内容 |
|---|---|---|
| - | 前提 | なし |
