<!-- コピーして 02_要件定義/01_機能要件/FR-XXX_要件名.md として使用。index.md への行追加を先に行うこと -->
<!-- 機能要件(FR)は「基本情報＋業務ルール(制約)＋受入条件＋システムユースケース(振る舞い・データ)」で構成する。入力/出力・事前/事後・フローはシステムユースケース節に集約し、要件本体との内容重複を作らない -->
<!-- 各セクション直上のコメントに「定義内容(そのセクションの意味)」「定義する条件」「項目説明(各列・各項目の意味)」「定義ルール」をセットで記載する。編集時はコメントを読んでから該当セクションを埋める -->

<!--
【タイトル】
定義内容: 文書のタイトル。「FR-XXX：<機能名> 仕様書」の形式で、H1(# )で記載する。
定義ルール:
- <機能名> は要件名に対応する機能の呼称(例: 会議室検索機能)。
- 以降のセクション見出しは H2(## )、サブセクションは H3(### )とする。
-->
# FR-XXX：<機能名> 仕様書

<!--
【1. 基本情報】
定義内容: この機能要件の識別情報(要件ID・要件名・概要・目的・対象ロール)を一覧で示す。
定義する条件: 全 FR で必須。
項目説明:
- 要件ID: この機能要件の識別子(FR-XXX 連番)。
- 要件名: 機能要件の日本語名称。
- 概要: この機能要件の内容(1〜3行)。
- 目的: この機能を提供する業務上のねらい・効果(1〜3行)。
- 対象ロール: この機能を利用する利用者区分(例: 利用者／管理者)。
定義ルール:
- 要件ID は FR-XXX の連番。採番は index.md の最大値 +1(欠番の再利用は禁止)。
- FR はトレース参照先を持たない(上流文書の ID を記載しない。用語・業務ルールの言及のみ可)。
- 概要・目的は1〜3行。画面項目・API仕様・DB項目・処理ロジックは書かない。
- 対象ロールは要件レベルの粗い利用者区分のみ記載し、画面のロール制御・API の認可仕様は書かない(それらは SCR / API 側で定義)。
-->
## 1. 基本情報
| 項目 | 内容 |
|---|---|
| 要件ID | FR-XXX |
| 要件名 |  |
| 概要 | (1〜3行) |
| 目的 | (1〜3行) |
| 対象ロール |  |

<!--
【2. 業務ルール（制約事項）】
定義内容: システムが遵守すべき業務上の絶対ルールと、この要件で扱わない範囲(制約)を1つの表に定義する。このルールが §4 システムユースケースのシナリオ分岐(代替・例外)の根拠となる。
定義する条件: 全 FR で必須。
項目説明:
- No: ルールの連番。
- ルール: 判断基準となる業務ルール、または「〜はこの要件では扱わない」等の制約・範囲外。
定義ルール:
- 業務上の判断基準(旧「業務ルール」)と、この要件の範囲・除外(旧「制限事項」)を統合し、絶対に守るべき制約を1覧にする。
- 画面・API・DBの実装詳細や処理ロジックは書かない(業務レベルのルールのみ)。
- §4 のシナリオ分岐(ALT/EXC)が根拠として参照できる粒度で、達成可否を判断できるように記載する。
-->
## 2. 業務ルール（制約事項）
システムが遵守すべき業務上の絶対ルールを定義する。このルールがユースケースのシナリオ分岐の根拠となる。

| No | ルール |
|---|---|
| 1 |  |

<!--
【3. 受入条件】
定義内容: この要件を満たしていることを確認する条件(テストの合否判定基準)を定義する。
定義する条件: 全 FR で必須。
項目説明:
- No: 条件の連番。
- 条件: 満たされていれば要件充足とみなせる、確認可能な条件。
定義ルール:
- テストの合否判定に使える粒度で記載する。画面・APIの実装詳細は書かない。
- §2 業務ルールおよび §4 システムユースケースと整合させる。
-->
## 3. 受入条件
この要件を満たしていることを確認する条件（テストの合否判定基準）を定義する。

| No | 条件 |
|---|---|
| 1 |  |

<!--
【4. システムユースケース】
定義内容: 要件を実現するための、利用者とシステムの具体的な振る舞い(シナリオ)と扱うデータを、ユースケース単位で定義する。データなどのパターンにより1要件が複数UCを持つ場合は、UCごとに ### サブセクションを並べる。
定義する条件: この要件に対応するシステムユースケースがある場合に定義する。無い場合は本節見出しの下に「なし(理由)」と記載する(例: リマインドは JOB で実現し利用シナリオを持たない)。
項目説明:
- 見出し: 「### 4.x 【UC-01】<ユースケース名>（<対象>向け）」。UC ID は機能要件ごとのローカル連番(UC-01, UC-02 …)で、他文書からは完全修飾 FR-XXX/UC-01 で参照する。SCR / API はこの完全修飾IDを「トレース元」として参照する。<対象>は主アクター(例: 一般ユーザー向け／管理者向け)。
- 各UC見出し直下の1行: そのユースケースの概要(どのパターン・誰向けか)。
- 事前条件: 開始前に成立している状態。事後条件: 正常完了後に成立している状態。(それぞれ別テーブルで定義する)
- 入力データ: この振る舞いが扱う入力(要否付き)。出力データ: 結果として提供する出力。(それぞれ別テーブルで定義する。UCごとに扱うデータが異なる点を表現する)
- 基本フロー: 正常系の Step 順の 操作/処理・結果。代替フロー(ALT-x): 正常系から外れるが目的を達成する別シナリオ。例外フロー(EXC-x): エラーで処理が中断するシナリオ(発生Step・条件・エラーメッセージ・対応)。
定義ルール:
- 1要件に複数UCがある場合は「### 4.1 …」「### 4.2 …」と UC 単位で並べ、UC間は水平線(---)で区切る。UCが1つでも本形式(### 4.1)に従う。
- 各UC内の 事前条件・事後条件・入力データ・出力データ・各フローは、それぞれ太字ラベル＋テーブルで個別に記載する(§8-2 テーブル原則)。
- UC ID は機能要件ごとの連番(UC-01)を維持し欠番の再利用は禁止(ID規則は docs/CLAUDE.md §4.2 ローカルID)。トレース元(FR)は記載しない(この節を内包する要件自身が実現元で自明・自己参照になる)。
- ALT の分岐Step・EXC の発生Step は基本フローの Step 番号に対応させる。ローカルID(ALT-x/EXC-x)は他文書から FR-XXX/UC-01/ALT-1・FR-XXX/UC-01/EXC-1 と完全修飾して参照する。
- 例外フローは1行1条件で個別に定義し(複合条件を「または」で束ねない)、各行に業務レベルのエラーメッセージ(利用者に伝える内容)を記載する。画面固有のMSG表示仕様(MSG-ID・体裁)は SCR 側で定義する。
- 画面項目定義・API パラメータ・DB 構造などの設計詳細は書かない(振る舞いとデータのみ)。
-->
## 4. システムユースケース
この要件を実現する振る舞いを、データなどのパターンに応じてユースケース単位で定義する。

### 4.1 【UC-01】<ユースケース名>（<対象>向け）
<このユースケースの概要を1行で記載する>

**事前条件**

| No | 条件 |
|---|---|
| 1 |  |

**事後条件**

| No | 条件 |
|---|---|
| 1 |  |

**入力データ**

| 情報 | 要否 | 内容 |
|---|---|---|
|  | 必須／任意 |  |

**出力データ**

| 情報 | 内容 |
|---|---|
|  |  |

**基本フロー**

| Step | アクター | 操作/処理 | 結果 |
|---|---|---|---|
| 1 |  |  |  |

**代替フロー**

| ALT ID | 分岐Step | 条件 | フロー |
|---|---|---|---|
| ALT-1 |  |  |  |

**例外フロー**

| EXC ID | 発生Step | 条件 | エラーメッセージ | 対応 |
|---|---|---|---|---|
| EXC-1 |  |  |  |  |

<!-- 複数UCを持つ要件は、上の「### 4.x 【UC-01】…」ブロックを UC 単位で連番追加し(UC-01, UC-02 …)、UC間を --- で区切る。UCを持たない要件は本節見出しの下に「なし(理由)」と記載する。 -->
