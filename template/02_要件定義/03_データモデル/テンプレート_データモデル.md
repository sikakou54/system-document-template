<!-- コピーして 02_要件定義/03_データモデル/MDL-XXX_○○情報.md として使用。index.md への行追加を先に行うこと -->
<!-- 本書はエンティティ(システムが取り扱う情報)ごとの「データ定義書」。データ項目・ライフサイクル(CRUD)・関連機能/業務ルール・セキュリティを定義する。物理名(英語のテーブル名・カラム名)・物理型は書かない(作成ルール13)。区分値・権限・データ型などは要件レベルの意味・区分で自己完結的に記載し、設計層のID(DEF-XXX/TBL-XXX 等)は参照しない(要件定義は機能設計を参照しない=逆参照禁止)。参照は同じ要件定義層の関連要件(FR/CFR/NFR)のみ -->
<!-- 各見出し(##)直上のコメントに「定義内容」「定義する条件」「項目説明」「定義ルール」をセットで記載する。編集時はコメントを読んでから該当セクションを埋める -->

<!--
【タイトル】
定義内容: 文書のタイトル。「エンティティ定義：<エンティティ名>」の形式で H1(# )で記載する。
定義ルール:
- <エンティティ名> は業務概念の日本語名(例: 会議室)。物理名(英語テーブル名)は書かない。
- 以降のセクション見出しは H2(## )とする。
-->
# エンティティ定義：<エンティティ名>

<!--
【1. 基本情報】
定義内容: このエンティティの識別情報(ID・概要・データ管理者)を示す。
定義する条件: 全 MDL で必須。
項目説明:
- エンティティID: 識別子(MDL-XXX 連番)。採番は index.md の最大値 +1(欠番の再利用は禁止)。
- 概要: このエンティティがどんなデータかを1〜2行で説明する。
- データ管理者: このデータの管理責任を持つ部門・ロール(例: 総務部門)。
定義ルール:
- 物理名(テーブル名)は書かない。
-->
## 1. 基本情報
* **エンティティID:** MDL-XXX
* **概要:** (1〜2行)
* **データ管理者:**

<!--
【2. データ項目定義】
定義内容: このエンティティが保持するデータ項目(属性)を定義する。
定義する条件: 全 MDL で必須。
項目説明:
- No: 連番。
- 属性名 (論理名): 日本語の項目名。
- データ型: 業務レベルの型区分(文字列 / 数値 / 日時 / 真偽 など)。
- 長さ: 想定する最大桁数・文字数。自動採番IDや桁数が主要でない項目は「-」でよい。
- 主キー: このエンティティを一意に識別する項目に〇、それ以外は「-」。
- 必須: 値が必須なら〇、任意なら「-」。
- 説明: 初期値・意味・区分値などを記載する。他エンティティを参照する項目(外部キー相当)はその旨を記載する。
定義ルール:
- 物理名(英語のカラム名)・物理型(varchar/int 等の英語表記)は書かない(作成ルール13)。データ型は業務レベルの型区分で記載する。
- 区分値は説明列に「`値`:意味」で業務レベルの意味として列挙する。設計層のID(DEF-XXX/CODE-XXX)は参照しない(要件定義は機能設計を参照しない=逆参照禁止)。
- 共通の作成日時・更新日時・削除日時などの管理項目は列挙しない。
- 他エンティティを参照する項目は説明にその関連を記載する。エンティティ間の全体関連は 概念ER図.md を参照する。
-->
## 2. データ項目定義
当エンティティが保持する具体的なデータ項目（属性）の詳細を定義する。

| No | 属性名 (論理名) | データ型 | 長さ | 主キー | 必須 | 説明 (初期値など) |
|:---|:---|:---|:---:|:---:|:---:|:---|
| 1 |  | 文字列 |  | 〇 | 〇 |  |

<!--
【3. データライフサイクル・制約事項（CRUD）】
定義内容: このデータの発生から廃棄までのルールやデータ操作における制約を、操作(CRUD)ごとにテーブルで定義する。
定義する条件: 全 MDL で必須。
項目説明:
- 操作: 作成 (Create) / 参照 (Read) / 更新 (Update) / 削除 (Delete)。
- ルール・制約: その操作を誰が・どの機能で・どのように行うか、および制約(論理削除など)。
定義ルール:
- 削除は原則、物理削除せず論理削除(削除済みとして扱い、検索・一覧に表示しない。RULE-COM-003)とする場合が多い。方式を明記する。
- 具体的な判定ロジック(SQL-ID・カラム名・具体値)は書かず、処理の目的で記載する(作成ルール11)。
-->
## 3. データライフサイクル・制約事項（CRUD）
本データの発生から廃棄までのルールやデータ操作における制約を定義する。

| 操作 | ルール・制約 |
|:---|:---|
| 作成 (Create) |  |
| 参照 (Read) |  |
| 更新 (Update) |  |
| 削除 (Delete) |  |

<!--
【4. 関連機能・業務ルール】
定義内容: このデータが影響を与える機能(関連要件)と、依存する業務ルールを、それぞれテーブルで定義する。
定義する条件: 全 MDL で必須。
構成: 「関連機能（機能要件）」と「業務ルールへの影響」の2つのテーブルに分割する。
項目説明:
- 関連機能（機能要件）: 関連要件ID(このデータを利用・実現する FR/CFR/NFR)＋要件名。
- 業務ルールへの影響: No＋内容(このデータが判定基準・制約として働く業務ルール)。
定義ルール:
- 参照する FR/CFR/NFR は実在するIDを記載する。設計ID(SCR/API/TBL 等)は記載しない。
-->
## 4. 関連機能・業務ルール
本データが影響を与える機能や、依存する業務ルールを定義する。

**関連機能（機能要件）**

| 関連要件ID | 要件名 |
|:---|:---|
|  |  |

**業務ルールへの影響**

| No | 内容 |
|:---|:---|
| 1 |  |

<!--
【5. セキュリティ・アクセス権限】
定義内容: このデータに対する参照・更新権限や機密性を、項目ごとにテーブルで定義する。
定義する条件: 全 MDL で必須。
項目説明:
- 参照権限: 参照できる利用者区分。
- 更新権限: 作成・更新・削除できる利用者区分。
- 機密性レベル: 低 / 中 / 高 と、その理由。
定義ルール:
- 参照・更新権限は要件レベルの利用者区分(例: 管理者／一般／本人／システム)で記載する。設計層のID(DEF-XXX/TBL-XXX 等)は参照しない(要件定義は機能設計を参照しない=逆参照禁止)。
- 具体的なロール物理名は書かず、業務上のロール呼称(例: 管理者権限)で記載する。
-->
## 5. セキュリティ・アクセス権限
本データに対する参照・更新権限や機密性を定義する。

| 項目 | 内容 |
|:---|:---|
| 参照権限 |  |
| 更新権限 |  |
| 機密性レベル |  |
