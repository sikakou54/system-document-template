<!-- コピーして 02_機能設計/01_概要設計/ 配下の概要設計文書として使用する。ファイル名は NN_名称.md(例: 01_システム概要図.md) -->
<!-- 概要設計はシステム全体の構成・構成要素の責務・外部連携・運用方針を俯瞰する。個別画面(SCR)・個別API(API)・DBカラム(TBL)の詳細は各設計文書が正本のため、本文書ではIDで参照し再記載しない -->
<!-- ERR/MSG は該当API・SCR文書のインライン定義、共通定義設計・共通カラム・API-COM などの共通仕様は各正本を、いずれも再記載せず ID 参照のみとする -->
<!-- 各見出し(##/###)直上のコメントに「定義内容(そのセクションの意味)」「定義する条件」「項目説明(各列・各項目の意味)」「定義ルール」をセットで記載する。編集時はコメントを読んでから該当セクションを埋める -->

<!--
【1. 概要】
定義内容: この概要設計文書が扱うシステム(または連携)の全体像・目的・採用技術基盤を1〜3行で示す。
定義する条件: 全概要設計文書で必須。冒頭に記載する。
項目説明:
- 概要本文: 対象システムの目的と、採用する実行基盤・外部サービスの要点(1〜3行)。
定義ルール:
- 1〜3行で簡潔に記載する。個別画面・API・DBカラムの仕様は書かない。
- 採用技術は正である技術前提(スタック)に一致させる。
-->
# 1. 概要

(対象システム/連携の全体像・目的・採用基盤を1〜3行で記載)

<!--
【2. システム構成図】
定義内容: 構成要素(フロント・API・DB・非同期処理・外部サービス)の関係を mermaid で俯瞰する。
定義する条件: 全概要設計文書で必須。
項目説明:
- 構成図: mermaid flowchart。subgraph で層(プラットフォーム/API/データ/非同期/外部)を区切り、要素間の依存・呼び出し方向を矢印で示す。
- ノードラベル: 「表示名<br/>関連ID(SCR-*/API-*/MOD-*/TBL-*/JOB-*)」の形で関連設計IDを併記する。
定義ルール:
- ラベルは必ず " で囲む(括弧・記号を含めても壊れないようにする)。
- 図に登場する各要素は §3 構成要素と責務で必ず説明する(図と表を一致させる)。
- 矢印は「呼び出す側 → 呼び出される側」の依存方向で描く。
-->
# 2. システム構成図

構成要素(フロント・API・モジュール・DB・非同期処理・外部サービス)の関係を示す。

```mermaid
flowchart LR
  A["表示名<br/>関連ID"] --> B["表示名<br/>関連ID"]
```

<!--
【3. 構成要素と責務】
定義内容: 構成図に登場する各構成要素の種別・関連設計ドキュメント・責務を一覧で示す。
定義する条件: 全概要設計文書で必須。構成図の全要素を過不足なく載せる。
項目説明:
- 構成要素: ブロック名(構成図のノードと一致させる)。
- 種別: 分類(基盤 / API / モジュール / データ / 非同期 / 外部サービス など)。
- 関連ドキュメント: 詳細仕様の正本となる設計ID群(SCR-*/API-*/MOD-*/TBL-*/JOB-*)。無ければ「-」。
- 責務: その要素が担う役割(1〜2行)。
定義ルール:
- 構成図に描いた要素はすべて表に載せる(図と表を一致させる)。
- 詳細仕様は関連ドキュメントIDで参照し、本表には再記載しない。
-->
# 3. 構成要素と責務

| 構成要素 | 種別 | 関連ドキュメント | 責務 |
|---|---|---|---|
|  |  |  |  |

<!--
【4. 外部連携】
定義内容: 外部サービスとの連携について、用途・連携方式・入出力・失敗時の方針・関連IDを一覧で示す。
定義する条件: 外部サービス連携がある場合に定義する。無ければ行に「なし」を記載する。
項目説明:
- 連携先/連携点: 外部サービス名と連携の単位(例: 決済サービスの決済セッション、メール送信サービスの送信)。
- 用途: その連携で実現すること。
- 連携方式: 呼び出し方向・方式(REST呼び出し / Webhook受信 / SDK など)と認証・検証方式。
- 入出力: 主な入力(送信データ)と出力(受信データ)。
- 失敗時の方針: 失敗時の扱い(リトライ・冪等・ERR-ID・通知)。
- 関連ID: この連携を担う設計ID(MOD-*/API-*/JOB-*/TBL-*)。
定義ルール:
- ERRコードは ERR-ID＋発生条件のみ記載する(エラー名・HTTPステータス・文言の定義は該当API文書のインライン定義／共通エラーは API-COM_共通設計.md を参照)。
- 連携の詳細な処理手順はモジュール設計(MOD)・API設計・JOB設計が正本。本表は連携方式と方針の俯瞰にとどめる。
- 外部サービス側のAPI項目名・イベント名・ヘッダ名など、物理名以外に定義する術がない場合は例外として最小限の物理名記載を許可する。
-->
# 4. 外部連携

| 連携先/連携点 | 用途 | 連携方式 | 入出力 | 失敗時の方針 | 関連ID |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

<!--
【5. 非機能・運用方針】
定義内容: システム全体に関わる非機能要求・運用方針(可用性・セキュリティ・バックアップ・監視・シークレット管理など)を、関連する要件IDとともに俯瞰する。
定義する条件: 全概要設計文書で必須。
項目説明:
- 観点: 非機能・運用の観点(可用性 / セキュリティ / データ保護 / 監査 / 運用 など)。
- 方針: その観点で採る構成・運用上の方針。
- 関連ID: 実現元の非機能要件ID(NFR-* / CFR-*)。無ければ「-」。
定義ルール:
- 非機能要件(NFR)の定義本体は 01_要件定義/03_非機能要件/ が正本。本表は方針の俯瞰にとどめ ID で参照する。
- 共通仕様(API-COM)・共通定義設計・共通カラム、およびエラー(該当API文書/API-COMのインライン定義)・メッセージ(各SCR文書のインライン定義)等の正本の内容は再記載しない。
-->
# 5. 非機能・運用方針

| 観点 | 方針 | 関連ID |
|---|---|---|
|  |  |  |
