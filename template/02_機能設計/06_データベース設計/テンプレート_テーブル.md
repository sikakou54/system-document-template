<!-- コピーして 02_機能設計/06_データベース設計/TBL-XXX_テーブル名.md として使用。02_機能設計/06_データベース設計/index.md への行追加を先に行うこと -->
<!-- 共通カラム(CREATED_AT, UPDATED_AT, DELETED_AT)は 共通カラム.md が正本。本文書には記載しない(主キー ID は §2 カラム定義に記載する) -->
<!-- 各見出し(##/###/####)直上のコメントに「定義内容(そのセクションの意味)」「定義する条件」「項目説明(各列・各項目の意味)」「定義ルール」をセットで記載する。子セクションを持つセクションは、親コメントにセクション全体の定義内容・共通ルールを、各子セクションのコメントにその子の項目説明を記載する。編集時はコメントを読んでから該当セクションを埋める -->

<!--
【1. 基本情報】
定義内容: このテーブルの識別情報(ID・物理名・論理名・概要)を一覧で示す。
定義する条件: 全テーブルで必須。
項目説明:
- テーブルID: このテーブルの識別子(TBL-XXX 連番)。
- 物理名: 実装上のテーブル名(実装キー)。
- 論理名: テーブルの日本語表示名。
- 概要: テーブルの目的(1〜3行)。
定義ルール:
- テーブルIDは TBL-XXX の連番。物理名は実装と一致させる。
- 論理名=日本語の表示名、物理名=実装キー(テーブルの物理名)。物理名(テーブル名)は大文字で、種別の接頭辞を付ける(M_=マスタ / T_=トランザクション / H_=履歴 / TP_=一時テーブル)。
- 論理名は種別を表す接尾辞を付ける(〜マスタ / 〜トランザクション / 〜履歴 / 〜一時テーブル)。
-->
## 1. 基本情報

| 項目 | 内容 |
|---|---|
| テーブルID | TBL-XXX |
| 物理名 |  |
| 論理名 |  |
| 概要 | (1〜3行) |

<!--
【2. カラム定義】
定義内容: このテーブルの各カラムを、キー種別・外部キー参照先まで含めて一括で定義する。
定義する条件: 全テーブルで必須。主キー(ID)と業務カラムを定義する。
項目説明:
- No: カラムの連番。
- 論理名: カラムの日本語表示名。
- 物理名: カラムの物理名(実装キー)。
- 型: カラムのデータ型(int / varchar(n) / datetime など)。
- NULL: NULL を許可するか(Yes / No)。
- PK: 主キーか(◯ / ー)。
- FK: 外部キーの場合、参照先「テーブル名.物理名」を記載する。外部キーでなければ空欄。
- Default: デフォルト値(無ければ空欄)。
- 補足: 補足事項があるカラムのみ記載する。外部キーは削除時挙動(RESTRICT / CASCADE / SET NULL)、区分値カラムは ENM-x(§5 列挙値の ID)を記載する。補足がなければ空欄。
定義ルール:
- 主キー(ID)は PK=◯ として先頭に記載する。CREATED_AT / UPDATED_AT / DELETED_AT は 共通カラム.md が正本のため記載しない。
- PK は ◯(主キー) / ー(非主キー) で表す。FK は参照先「テーブル名.物理名」を記載し、削除時挙動は補足列に記載する。一意制約は §3 インデックスの一意インデックス(Unique=Yes)で表す。
- テーブル名・カラム名・インデックス名(物理名)はすべて大文字で記載する。
- 区分値(Enum)を持つカラムは、型を ENUM とし、補足列に ENM-x を記載する。値の一覧は §5 列挙値 で定義する。
-->
## 2. カラム定義

<!-- 主キー ID は記載する。CREATED_AT / UPDATED_AT / DELETED_AT は記載しない。PK / FK は ◯/ー -->

| No | 論理名 | 物理名 | 型 | NULL | PK | FK | Default | 補足 |
|---|---|---|---|---|---|---|---|---|
| 1 | ID | ID | bigint | No | ◯ |  | AUTO_INCREMENT |  |
| 2 |  |  |  | No | ー |  |  |  |

<!--
【3. インデックス】
定義内容: このテーブルに定義するインデックスを、ローカルID・対象カラム・一意性・用途で定義する。
定義する条件: 検索・整合性のためインデックスが必要な場合に定義する。無ければ行を空のままにする。
項目説明:
- IDX ID: インデックスのローカルID(IDX-1 形式)。
- インデックス名: 実装上のインデックス名(物理名)。
- 対象カラム: インデックスを張るカラム(複合は先頭から順に列挙)。
- Unique: 一意インデックスか(Yes / No)。
- 用途: このインデックスの目的(どの検索・制約を支えるか)。
定義ルール:
- ローカルIDは IDX-1 形式の連番。他文書からは TBL-XXX/IDX-1 で完全修飾して参照する。
- インデックス名は実装と一致する物理名を大文字で記載する(命名例: IDX_テーブル_カラム、一意は UX_)。
- 複合インデックスは対象カラムを先頭から順に記載する。
-->
## 3. インデックス

| IDX ID | インデックス名 | 対象カラム | Unique | 用途 |
|---|---|---|---|---|
| IDX-1 |  |  | Yes / No |  |

<!--
【4. 制約】
定義内容: 主キー・外部キー以外に、このテーブルが満たすべき制約(複合一意・チェックなど、単一カラムで表せないもの)を定義する。
定義する条件: 複数カラムにまたがる制約がある場合に定義する。無ければ「なし」の行を残す。
項目説明:
- 制約: 制約の種別(UNIQUE / CHECK など)。
- 内容: 制約が成立とみなす条件。
定義ルール:
- 単一カラムで表せる制約(NOT NULL・型・単一カラムの一意)はカラム定義・インデックスで表し、本表には複数カラムにまたがる制約を記載する。
- 大小・前後の比較は ＜ / ＜＝ / ＞ / ＞＝ で表す。
-->
## 4. 制約

| 制約 | 内容 |
|---|---|
| - | なし |

<!--
【5. 列挙値】
定義内容: このテーブルのカラムが使用する ENUM(区分値)を、値・表示名・説明まで定義する(この Enum を使うテーブル文書が正本)。
定義する条件: 区分値を格納するカラムがある場合に定義する。無ければセクションを空にする。
構成: 各 Enum を ### ENM-x Enum名 の見出しで定義する。各 Enum の項目説明は直下のコメントを参照する。
定義ルール(セクション共通):
- ENM-ID は文書内で ENM-1 からの連番。他文書からは TBL-XXX/ENM-x で完全修飾して参照する。
- Enum を使うカラムがあるテーブル文書で値を定義し、他文書は TBL-XXX/ENM-x で参照する(値の再記載はしない)。
- コード値の削除・再利用は禁止(行の追加のみ)。§2 カラム定義の該当カラムと対応させる。
-->
## 5. 列挙値

<!--
【### ENM-x Enum名】
定義内容: 1つの区分値(Enum)の値・表示名・説明を定義する。
定義する条件: カラムが使用する区分値ごとに定義する。
項目説明:
- ENM-ID / Enum名: 区分値の文書内ローカルID(ENM-1 形式)と日本語名。見出し(### ENM-x Enum名)に記す。
- 値: DB に格納するコード値。
- 表示名: 値の日本語表示名。
- 説明: 値の意味。
-->
### ENM-x Enum名

| 値 | 表示名 | 説明 |
|---|---|---|
| 1 |  |  |
