<!-- コピーして docs/04_トレーサビリティ/traceability_<経路>.md として使用 -->
<!-- 対応表は各文書の基本情報欄(トレース欄・依存欄)の転記のみで構成する(独自情報を書かない)。リンクの正本は各文書側であり、本対応表は逆方向追跡のための転記(docs/CLAUDE.md §6) -->
<!-- 各セクション直上のコメントに「定義内容(そのセクションの意味)」「定義する条件」「項目説明(各列・各項目の意味)」「定義ルール」をセットで記載する。編集時はコメントを読んでから該当セクションを埋める -->

<!--
【1. 概要】
定義内容: この対応表が管理するトレース経路(例: BR→FR/CFR→UC)と追跡目的を要約する。
定義する条件: 全トレーサビリティ文書で必須。冒頭に 1〜3 行で記載する。
項目説明:
- 対象経路: この表がつなぐ文書種別の連鎖(例: SCR→API→MOD→TBL)。
- 追跡目的: この表で何を辿れるようにするか(上流→下流／下流→上流)。
- 転記元: 各列が転記する元文書(基本情報欄)の所在。
定義ルール:
- 1〜3行で簡潔に記載する。個別文書の仕様や表独自の情報は書かない。
- 参照方向・トレーサビリティの正本ルールは docs/CLAUDE.md §5・§6。ここでは経路と目的のみ示す。
-->
# 1. 概要

(この対応表がつなぐ経路と追跡目的を1〜3行で記載)

<!--
【2. 対応表】
定義内容: 起点IDから終点IDまでのトレース／依存の対応を、各文書の基本情報欄の転記のみで 1 行ずつ一覧化する。
定義する条件: 全トレーサビリティ文書で必須。関連文書を作成・更新したら同一作業内で該当行を更新する。
項目説明(列定義):
- 先頭列: 起点となる文書のID(例: BR ID / UC ID / FR ID / SCR ID)。1行1起点。
- 中間・末尾のID列: 起点が実現・呼び出す下流ID、または起点が適用する上流ID(例: 実現FR・CFR / 関連UC / API / MOD / 主なTBL)。複数ある場合はカンマ区切り。該当なしは「-」。
- 備考: 転記上の補足(共有・画面外・欠番など)。任意。不要なら「-」。
定義ルール:
- 各列は関連文書の基本情報欄(トレース元・依存・使用API等)からの転記のみとする。表に独自の判断・新規IDを書かない。
- 記載するIDはすべて実在するもの(該当index.md／各文書のインライン定義に存在)に限る。存在しないIDを作らない。
- ID採番・欠番・廃止の扱いは docs/99_その他/ID採番ルール に従う。廃止IDは行を残し備考に「廃止」と記す。
- 列構成は経路に応じて定義する(先頭=起点ID、以降=トレース／依存先ID、末尾=備考(任意))。標準の「起点ID」列と「備考」列は削除しない。
-->
# 2. 対応表

<!-- 列は経路に応じて定義する。下は列定義の例(実文書では経路に合わせて置き換える) -->

| 起点ID | トレース／依存先ID | 備考 |
|---|---|---|
| XXX-001 | YYY-001 | - |

<!--
【3. 更新ルール】
定義内容: この対応表をいつ・どのように更新するかを定義する。
定義する条件: 全トレーサビリティ文書で必須。
項目説明:
- 更新契機: 行を更新すべきタイミング。
- 更新内容: 何を転記／修正するか。
定義ルール:
- 関連文書(要件・設計)の作成・更新と同一作業内で、対応する行を必ず更新する(docs/CLAUDE.md §6・§10-7)。
- 更新ルールの正本は docs/CLAUDE.md §6。ここではその適用のみを記し、ルールを再定義しない。
-->
# 3. 更新ルール

| 更新契機 | 更新内容 |
|---|---|
| 関連文書の新規作成 | 起点IDの行を追加し、トレース／依存先IDを各文書の基本情報から転記する |
| 関連文書のトレース元・依存の変更 | 該当行のID列を最新の基本情報に合わせて更新する |
| ID廃止 | 行は削除せず、備考に「廃止」と記す |

- 本対応表の更新は関連文書の作成・更新と同一作業内で行う(正本: docs/CLAUDE.md §6・§10-7)。
