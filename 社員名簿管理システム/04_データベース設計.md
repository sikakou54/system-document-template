[← 設計書一覧（社員名簿管理システム）](README.md)

# 4. データベース設計

## 4.1 採用DBMS・物理設計方針

業務データの論理構造(エンティティ・属性・関連)は§2.3 データモデルを正本とし、本章はそのSQLite物理構造を詳細化する。DB要素名と接続境界は§4.1・§4.2を正本とする。M-006以外からD1へ到達する経路を追加しない。

| 項目 | 設計 |
|---|---|
| DBMS / SQL方言 | Cloudflare D1 / SQLite互換方言 |
| 実行環境 | Cloudflare Workers PaidのD1 Binding。SQL実行はM-006 D1データアクセスに限定する |
| テーブル型制約 | EMPLOYEE〜AUDIT_LOGの通常テーブルとTX_MUTATION_GUARD技術テーブルはすべて`CREATE TABLE ... STRICT`で作成する。任意導入のFTS5 virtual tableだけは`STRICT`対象外 |
| 検索機能 | 初期正本はSQLite組込み関数の`instr(lower(対象列), lower(term)) > 0`を使用する。FTS5 `trigram`は性能試験で移行基準を超えた場合だけ採用する任意候補とし、初期マイグレーションには含めない |
| 文字コード | UTF-8 |
| タイムゾーン | DB保存はUTC、業務日判定と画面表示はAsia/Tokyo。DB側の現在時刻関数を保存値に直接使用せず、M-006がUTCのISO 8601値をバインドする |
| 日付 | `TEXT`。`YYYY-MM-DD`のISO形式で保存し、文字列順を日付順として利用する。有効期間の開始日・終了日は両端を含む |
| 日時 | `TEXT`。UTCの`YYYY-MM-DDTHH:mm:ss.SSSZ`形式で保存する |
| 主キー | UUIDの正準表現を`TEXT`で保存する。Workers側のモジュールで採番して登録する |
| 真偽値 | `INTEGER NOT NULL CHECK (値 IN (0, 1))`。`1`を真、`0`を偽とする |
| JSON | `TEXT`で保存し、`json_valid()`と用途別`json_type()`をCHECKする。業務概要は`object`、更新ガードのID集合は`array`とする |
| 楽観ロック | 更新対象の`version`一致をWHERE条件に含め、成功時に1加算する |
| 論理削除 | マスター・利用者等は`deleted_at IS NULL`を有効行条件とする。社員は在籍状態で管理し物理削除しない |
| マスター利用可否 | ORGANIZATION・POSITIONの`active`は有効期間と独立した手動利用可否。指定日利用可は`active = 1 AND effective_from <= 指定日 AND (effective_to IS NULL OR 指定日 <= effective_to)`。未来開始前・期限到来後でも`active = 1`は許容する |
| 更新単位 | 参照は`first()`/`all()`、単一文更新は`run()`、複数文の業務更新はM-006がprepared statementを`D1Database.batch()`へ順番どおりに渡す。D1は全statement成功時だけ確定し、statement例外時はバッチ全体を取り消す |
| 競合制御 | D1単一DBの書込み逐次性を前提とし、分離レベル指定やロック構文は使用しない。所有者`version`ガード、DB制約、検証トリガー、更新対象ID集合の完全一致検証で事前参照後の状態変化を検出する |
| 再試行 | 即時再試行対象は§6.3.5に列挙した5エラーだけとし、指数バックオフ＋full jitterで初回後最大2回再実行する。書込結果不明は状態・versionを再読込してcommit確認後、未反映だけ再試行する。overload、timeout、CPU・memory超過は即時再試行しない。M-006は論理一時障害を返し、M-002がオンライン要求を503系、JOBをQueue再配信へ振り分ける |
| 実行予算 | Workers Paid、D1 Paid上限1,000 statement/Worker invocationに対し内部上限900、ordered placeholderは1 statement最大100個。M-006が実測statement数をM-002へ返す |
| JOB基盤 | Cron scheduledハンドラーはCloudflare Queuesへ初回メッセージを投入するだけとし、queueハンドラーが1メッセージにつきJOB-001を1回、JOB-001がM-002/IF-07を1回呼ぶ。`max_batch_size=1`、`max_concurrency=1`、`max_retries=3`、超過時はDLQへ送る |
| DBアクセス境界 | M-006 D1データアクセスだけがSQLを実行する。API・JOB・他モジュールによる直接アクセスは禁止する |

### 4.1.1 論理型・SQLite物理型マッピング

| 論理値 | SQLite物理型 | 保存形式・制約 | M-006変換時の注意 |
|---|---|---|---|
| ID（UUID） | TEXT | UUID正準表現。`length(列) = 36`・8-4-4-4-12のハイフン・英小文字16進のCHECK、PK/UNIQUE | 文字列のままバインドし数値へ変換しない |
| 文字列 | TEXT | NOT NULL、`CHECK (length(列) BETWEEN 1 AND 最大長)`、前後空白除去・NFC正規化済み | `undefined`をバインドせず、未指定とNULLを公開IFで区別する |
| 整数 | INTEGER | 範囲CHECK（`version >= 1`、`position_level >= 0`等） | JavaScriptの安全整数範囲で扱う |
| 真偽値 | INTEGER | `CHECK (列 IN (0, 1))`、1=真 / 0=偽 | M-006がBoolean⇔0/1を変換する |
| 日付 | TEXT | `YYYY-MM-DD`、`strftime('%Y-%m-%d', 列) = 列`のCHECK。有効期間は両端を含む | 比較前に同一書式へ正規化する |
| 日時 | TEXT | UTCの`YYYY-MM-DDTHH:mm:ss.SSSZ`のCHECK | M-006が確定UTC値をバインドし、DBの現在時刻関数を保存値へ使わない |
| JSON | TEXT | `json_valid(列)`と用途別`json_type(列)`（業務概要=object、更新ガードID集合=array） | stringify/parse失敗を論理データアクセス例外へ変換する |

`VARCHAR`・`UUID`・`BOOLEAN`・`DATE`・`TIMESTAMP`・`BIGINT`・配列・`JSONB`等を物理型に使用せず、型名だけで制約されたとみなさずSTRICT・NOT NULL・CHECK・UNIQUEで保存条件を実装する。

### 4.1.2 M-006 bind・物理型対応

M-006はSQL-IDごとの「固定バインド順」を唯一のbind正本とし、順序付き`?1`〜`?N`と論理入力・SQLite値型の対応を§8の各SQL表で定義する。本節は共通変換規約を示し、SQL単位の正逆照合は§8を正本とする。

| SQLite値型 | 対象論理値 | `.bind()`値の生成・NULL方針 |
|---|---|---|
| TEXT | ID・文字列・日付・日時・JSON文字列 | 正規化済み確定値を文字列でバインドする。NULL可列だけ`null`を許可し、`undefined`を渡さない |
| INTEGER | 真偽（0/1）・版数・件数・`limit`/`offset`・役職レベル | Booleanは0/1へ変換し、整数値をそのままバインドする |

- placeholderを使うSQLは`?1`から最大ordinal`?N`まで欠番なく採番し、`.bind()`引数数をNと一致させる（§8.1・§8.1.2）。
- placeholderを持たないSQLはbindなしとし、名前付き`:name` / `@name` / `$name`と匿名`?`を実SQLへ使用しない。物理列型・CHECK・NULL可否と本表のSQLite値型・変換を一致させる。

## 4.2 テーブル一覧

| TBL-ID | テーブル名 | 論理名 | 対応エンティティ(§2.3) | 種別 | STRICT | 目的 |
|---|---|---|---|---|---|---|
| TBL-001 | EMPLOYEE | 社員 | 社員 | トランザクション | Yes | 社員の基本情報、在籍状態、退職予定を保持する |
| TBL-002 | EMPLOYEE_ASSIGNMENT | 社員所属履歴 | 社員所属履歴 | 履歴 | Yes | 所属組織・役職・上長の有効期間履歴を保持する |
| TBL-003 | ORGANIZATION | 組織 | 組織 | マスター | Yes | 組織階層と有効期間を保持する |
| TBL-004 | POSITION | 役職 | 役職 | マスター | Yes | 役職、役職レベル、有効期間を保持する |
| TBL-005 | EMPLOYEE_CHANGE_HISTORY | 社員変更履歴 | 社員変更履歴 | 追記履歴 | Yes | 業務上参照する社員変更概要を保持する |
| TBL-006 | USER_ACCOUNT | 利用者アカウント | 利用者アカウント | マスター | Yes | 外部認証主体と社員を対応付け、システム利用状態を保持する |
| TBL-007 | ROLE | ロール | ロール | マスター | Yes | システム上の役割を保持する |
| TBL-008 | USER_ROLE | 利用者ロール | 利用者ロール | 有効期間履歴 | Yes | 利用者へのロール割当と有効期間を保持する |
| TBL-009 | AUDIT_LOG | 監査ログ | 監査ログ | 追記ログ | Yes | 操作証跡とログ相関情報を保持する |
| TBL-010 | TX_MUTATION_GUARD | 更新ガード | −（技術テーブル。§2.3対象外） | 技術テーブル | Yes | 所有者版数と更新対象ID集合をD1バッチ内で再検証する。成功バッチの末尾で必ず削除する |

## 4.3 TBL-001 `EMPLOYEE`

### 4.3.1 基本情報

| 項目 | 内容 |
|---|---|
| TBL-ID / 論理名 | TBL-001 / 社員 |
| 目的 / 種別 | 社員の基本情報・在籍状態・退職予定を保持する / トランザクション |
| DDL | §4.16 MIG-001でCHECK・UNIQUE・外部キー付きの`STRICT`型付きtableとして作成し、インデックス・トリガーは§4.12（MIG-002）に従う |
| 更新主体 | M-006/IF-05(SQL-005 登録)・IF-08(SQL-008 基本情報更新)・IF-11(SQL-011 退職予定)・IF-13(SQL-013 退職確定)・IF-34(SQL-034 異動版数) |
| 保持・削除 | 在籍状態(status)と退職日で在籍を管理し、物理削除しない(§4.1)。個人情報は退職後も監査・履歴のため保持し、廃棄方針は運用規程に従う |

### 4.3.2 カラム定義

| カラム | 論理名 | SQLite型 | NULL | DEFAULT | 制約・保存形式 |
|---|---|---|---|---|---|
| employee_id | 社員ID | TEXT | 不可 | なし | PK。UUID正準表現 |
| employee_number | 社員番号 | TEXT | 不可 | なし | 最大20文字、UNIQUE。会社内で一意 |
| last_name | 姓 | TEXT | 不可 | なし | 最大100文字。Unicode前後空白を除去し、NFC正規化済みの空でない値を保存 |
| first_name | 名 | TEXT | 不可 | なし | 最大100文字。Unicode前後空白を除去し、NFC正規化済みの空でない値を保存 |
| last_name_kana | 姓カナ | TEXT | 可 | なし | 最大100文字。提供時はUnicode前後空白除去・NFC正規化済みの空でない全角カナ。消去はNULL |
| first_name_kana | 名カナ | TEXT | 可 | なし | 最大100文字。提供時はUnicode前後空白除去・NFC正規化済みの空でない全角カナ。消去はNULL |
| email | メールアドレス | TEXT | 不可 | なし | 最大254文字。大文字小文字を正規化した値にUNIQUE |
| phone_number | 連絡先電話番号 | TEXT | 可 | なし | 最大30文字。前後空白を除去して保存。NULLは未登録・解除済み |
| hire_date | 入社日 | TEXT | 不可 | なし | `YYYY-MM-DD` |
| retirement_date | 退職日 | TEXT | 可 | なし | `YYYY-MM-DD`。未来日の場合は退職予定。到来前はstatusをACTIVEのまま保持 |
| retirement_type_code | 退職区分 | TEXT | 可 | なし | 最大30文字。`VOLUNTARY` / `COMPANY` / `RETIREMENT_AGE` / `CONTRACT_END` / `OTHER`。未指定はNULL |
| employment_type_code | 雇用区分 | TEXT | 不可 | なし | 最大30文字。`REGULAR` / `CONTRACT` / `PART_TIME` / `TEMPORARY` / `OTHER` |
| status | 在籍状態 | TEXT | 不可 | なし | 最大20文字。`ACTIVE` / `RETIRED` |

### 4.3.3 キー・参照・不変条件

| 制約ID | 種別 | 対象・条件 | 違反時のM-006論理結果 |
|---|---|---|---|
| PK-001-01 | PRIMARY KEY | `(employee_id)` | CONSTRAINT_VIOLATION |
| UQ-001-01 | UNIQUE | `(employee_number)`（IDX-001参照。無条件UNIQUE） | DUPLICATED |
| UQ-001-02 | UNIQUE | `(lower(email))`（IDX-002参照。大文字小文字非依存） | DUPLICATED（IF-08はEMAIL_DUPLICATEDへ写像） |
| CK-001-01 | CHECK | `employee_id`のUUID形式（`length(employee_id) = 36`・8-4-4-4-12のハイフン・英小文字16進） | CONSTRAINT_VIOLATION |
| CK-001-02 | CHECK | 可変長TEXT列の`length(列) BETWEEN 1 AND 最大長`（employee_number=20 / last_name・first_name・last_name_kana・first_name_kana=100 / email=254 / phone_number=30 / retirement_type_code・employment_type_code=30 / status=20。NULL可列は`列 IS NULL OR ...`） | CONSTRAINT_VIOLATION |
| CK-001-03 | CHECK | `hire_date`・`retirement_date`の`YYYY-MM-DD`形式（`length(列) = 10`・`strftime('%Y-%m-%d', 列) = 列`。retirement_dateはNULL許可） | CONSTRAINT_VIOLATION |
| CK-001-04 | CHECK | `retirement_date IS NULL OR hire_date <= retirement_date` | CONSTRAINT_VIOLATION（退職日業務条件はINVALID_RETIREMENT_DATEへ写像） |
| CK-001-05 | CHECK | `status = 'RETIRED'`のとき`retirement_date IS NOT NULL` | CONSTRAINT_VIOLATION（退職日業務条件はINVALID_RETIREMENT_DATEへ写像） |
| CK-001-06 | CHECK | `status IN ('ACTIVE', 'RETIRED')` | CONSTRAINT_VIOLATION |
| CK-001-07 | CHECK | `employment_type_code IN ('REGULAR', 'CONTRACT', 'PART_TIME', 'TEMPORARY', 'OTHER')` | CONSTRAINT_VIOLATION |
| CK-001-08 | CHECK | `retirement_type_code IS NULL OR retirement_type_code IN ('VOLUNTARY', 'COMPANY', 'RETIREMENT_AGE', 'CONTRACT_END', 'OTHER')` | CONSTRAINT_VIOLATION |
| CK-001-09 | CHECK | 共通カラム（§4.14）の`version >= 1` | CONSTRAINT_VIOLATION |
| CK-001-10 | CHECK | 日時TEXT列`created_at`・`updated_at`・`deleted_at`（§4.14）のUTC`YYYY-MM-DDTHH:mm:ss.SSSZ`形式（deleted_atはNULL許可） | CONSTRAINT_VIOLATION |
| TRG-002 | TRIGGER | EMPLOYEE UPDATE(`retirement_date`/`status`/`deleted_at`)で、退職日当日以後に有効な部下からの上長参照が残らないこと（§4.12.4・物理名`trg_employee_manager_reference_bu`）。違反時`RAISE(ABORT, 'MANAGER_REFERENCE_EXISTS')` | MANAGER_REFERENCE_EXISTS（IF-11/IF-13はINVALID_RETIREMENT_DATEへ写像） |

- 本表はインライン外部キーを持たない。共通カラム`created_by`・`updated_by`（§4.14）はUUID正準表現を保持するが外部キー制約は課さない。
- 期間重複防止（§4.12.3）と参照整合トリガー群のうち、EMPLOYEEを主体(`ON EMPLOYEE`)とするのはTRG-002だけである。TRG-001（§4.12.4）はEMPLOYEE_ASSIGNMENTのINSERT/UPDATEでEMPLOYEEを上長参照先として検証する。

## 4.4 TBL-002 `EMPLOYEE_ASSIGNMENT`

### 4.4.1 基本情報

| 項目 | 内容 |
|---|---|
| TBL-ID / 論理名 | TBL-002 / 社員所属履歴 |
| 目的 / 種別 | 所属組織・役職・上長の有効期間履歴を保持する / 履歴 |
| DDL | §4.16 MIG-001でCHECK・UNIQUE・外部キー付きの`STRICT`型付きtableとして作成し、インデックス・トリガーは§4.12（MIG-002）に従う |
| 更新主体 | M-006/IF-05(SQL-006 初期所属登録)・IF-34(SQL-006 新所属登録・SQL-010 基準日終了/将来取消)・IF-13(SQL-010 基準日終了/将来取消)。いずれも合成公開IFの内部構成SQL |
| 保持・削除 | §4.14の論理削除(deleted_at)。将来所属の取消時にdeleted_atを設定し、履歴行を物理削除しない |

### 4.4.2 カラム定義

| カラム | 論理名 | SQLite型 | NULL | DEFAULT | 制約・保存形式 |
|---|---|---|---|---|---|
| assignment_id | 所属履歴ID | TEXT | 不可 | なし | PK。UUID正準表現 |
| employee_id | 社員ID | TEXT | 不可 | なし | FK → EMPLOYEE |
| organization_id | 組織ID | TEXT | 不可 | なし | FK → ORGANIZATION |
| position_id | 役職ID | TEXT | 不可 | なし | FK → POSITION |
| manager_employee_id | 上長社員ID | TEXT | 可 | なし | FK → EMPLOYEE。本人自身は指定不可。初期所属はNULL |
| effective_from | 適用開始日 | TEXT | 不可 | なし | `YYYY-MM-DD`。入社日または異動日 |
| effective_to | 適用終了日 | TEXT | 可 | なし | `YYYY-MM-DD`。現行所属はNULL |

### 4.4.3 キー・参照・不変条件

| 制約ID | 種別 | 対象・条件 | 違反時のM-006論理結果 |
|---|---|---|---|
| PK-002-01 | PRIMARY KEY | `(assignment_id)` | CONSTRAINT_VIOLATION |
| UQ-002-01 | UNIQUE | IDX-005（`EMPLOYEE_ASSIGNMENT(employee_id) WHERE effective_to IS NULL AND deleted_at IS NULL`のPARTIAL UNIQUE）参照。有効な現行所属を社員ごとに1件へ制約 | CONSTRAINT_VIOLATION |
| FK-002-01 | FOREIGN KEY | `(employee_id) REFERENCES EMPLOYEE(employee_id)`、ON DELETE RESTRICT | CONSTRAINT_VIOLATION |
| FK-002-02 | FOREIGN KEY | `(organization_id) REFERENCES ORGANIZATION(organization_id)`、ON DELETE RESTRICT | CONSTRAINT_VIOLATION |
| FK-002-03 | FOREIGN KEY | `(position_id) REFERENCES POSITION(position_id)`、ON DELETE RESTRICT | CONSTRAINT_VIOLATION |
| FK-002-04 | FOREIGN KEY | `(manager_employee_id) REFERENCES EMPLOYEE(employee_id)`、ON DELETE RESTRICT | CONSTRAINT_VIOLATION |
| CK-002-01 | CHECK | UUID形式（assignment_id・employee_id・organization_id・position_id・manager_employee_id：`length(列) = 36`・8-4-4-4-12ハイフン・英小文字16進。NULL可列はNULL許可。§4.12.1） | CONSTRAINT_VIOLATION |
| CK-002-02 | CHECK | 日付形式（effective_from・effective_to：`length(列) = 10`・`YYYY-MM-DD`・`strftime('%Y-%m-%d', 列) = 列`。NULL可列はNULL許可。§4.12.1） | CONSTRAINT_VIOLATION |
| CK-002-03 | CHECK | 期間順序 `effective_to IS NULL OR effective_from <= effective_to`（ISO日付TEXTの文字列比較。§4.12.1） | CONSTRAINT_VIOLATION |
| CK-002-04 | CHECK | 自己参照禁止 `manager_employee_id IS NULL OR manager_employee_id <> employee_id`（§4.12.1） | CONSTRAINT_VIOLATION |
| CK-002-05 | CHECK | 版数範囲 `version >= 1`（§4.14共通カラム） | CONSTRAINT_VIOLATION |
| CK-002-06 | CHECK | 日時形式（created_at・updated_at・deleted_at：UTCの`YYYY-MM-DDTHH:mm:ss.SSSZ`。NULL可列はNULL許可。§4.12.1・§4.14共通カラム） | CONSTRAINT_VIOLATION |
| TRG-001 | TRIGGER | §4.12.4 TRG-001（`trg_assignment_reference_bi`/`_bu`、BEFORE INSERT/UPDATE ON EMPLOYEE_ASSIGNMENT）。組織・役職が`active = 1`で所属期間全体を包含し、上長が本人以外で在籍し上長退職日の前日までに所属が終了することを再検証し、違反時 `RAISE(ABORT, 'ASSIGNMENT_REFERENCE_INVALID')` | ASSIGNMENT_REFERENCE_INVALID（M-006がCONSTRAINT_VIOLATIONへ写像） |
| TRG（§4.12.3） | TRIGGER | §4.12.3 期間重複防止（`trg_assignment_period_bi`/`_bu`、BEFORE INSERT/UPDATE ON EMPLOYEE_ASSIGNMENT）。終了日NULLを無限未来とみなし両端包含で同一社員の有効期間重複を検出し、違反時 `RAISE(ABORT, 'ASSIGNMENT_PERIOD_OVERLAP')` | ASSIGNMENT_PERIOD_OVERLAP（M-006がCONSTRAINT_VIOLATIONへ写像） |

上長社員の退職日当日以後に本表を上長参照する行を禁止する不変条件はEMPLOYEE側のTRG-002（`trg_employee_manager_reference_bu`、`MANAGER_REFERENCE_EXISTS`）、参照先組織の無効化・期間短縮・孤児化拒否はORGANIZATION側のTRG-003（`ORGANIZATION_REFERENCE_INVALID`）、参照先役職の無効化・期間短縮拒否はPOSITION側のTRG-004（`POSITION_REFERENCE_INVALID`）が各表のトリガーとして担保する（§4.12.4）。

### 期間整合性

- 同一社員の有効期間は重複させない。
- 基準日時点の所属は`effective_from <= 基準日 AND (effective_to IS NULL OR effective_to >= 基準日)`で判定する。
- `effective_to IS NULL`は無期限の最終行を表し、将来開始の予約所属を含めて社員ごとに最大1件とする。
- 異動可否判定では基準日に有効な行だけでなく、基準日以降に終了していない現在・将来行を取得して期間重複を検証する。
- 異動時は異動日の直前に有効な所属を異動日前日で終了し、異動日以降に開始する既存の将来所属を論理削除してから新所属を登録する。これを指定日以降の将来予約置換の正本とする。
- 即時・到来退職時は退職日に有効な所属を退職日で終了し、退職日より後に開始する将来所属を論理削除する。
- 上長社員の退職日当日以後も有効となる`manager_employee_id`参照が1件でもある場合、その退職予定・確定を拒否し、部下の上長変更を先に行う。
- 所属が参照する組織・役職は所属期間全体で利用可能でなければならない。マスター無効化・期間短縮は、現在・将来所属の参照期間を外す場合に拒否する。
- 終了日がある場合は`effective_from <= effective_to`を満たす。

## 4.5 TBL-003 `ORGANIZATION`

### 4.5.1 基本情報

| 項目 | 内容 |
|---|---|
| TBL-ID / 論理名 | TBL-003 / 組織 |
| 目的 / 種別 | 組織階層と有効期間を保持する / マスター |
| DDL | §4.16 MIG-001でCHECK・UNIQUE・外部キー付きの`STRICT`型付きtableとして作成し、インデックス・トリガーは§4.12（MIG-002）に従う |
| 更新主体 | M-006/IF-19(SQL-019 登録)・IF-20(SQL-020 更新) |
| 保持・削除 | §4.14の論理削除(deleted_at)またはactive=0による無効化。物理削除しない |

### 4.5.2 カラム定義

| カラム | 論理名 | SQLite型 | NULL | DEFAULT | 制約・保存形式 |
|---|---|---|---|---|---|
| organization_id | 組織ID | TEXT | 不可 | なし | PK。UUID正準表現 |
| organization_code | 組織コード | TEXT | 不可 | なし | 最大30文字、UNIQUE |
| organization_name | 組織名 | TEXT | 不可 | なし | 最大200文字 |
| parent_organization_id | 上位組織ID | TEXT | 可 | なし | FK → ORGANIZATION。自組織指定不可 |
| effective_from | 有効開始日 | TEXT | 不可 | なし | `YYYY-MM-DD` |
| effective_to | 有効終了日 | TEXT | 可 | なし | `YYYY-MM-DD`。期限なしはNULL |
| active | 手動利用可否 | INTEGER | 不可 | なし | `CHECK (active IN (0, 1))`。有効期間と独立。1でも未来開始前・期限到来後は指定日利用不可、DISABLE時は即時0 |

組織階層では、上位組織を指定する子組織が`active = 1`である間、上位組織も`active = 1`であり、かつ上位組織の有効期間が子組織の有効期間全体を包含しなければならない。組織更新は同一D1バッチ内の検証トリガーで階層を再帰評価し、循環に加えて、無効化・期間短縮・親変更による有効な子の孤児化を拒否する。

### 4.5.3 キー・参照・不変条件

| 制約ID | 種別 | 対象・条件 | 違反時のM-006論理結果 |
|---|---|---|---|
| PK-003-01 | PRIMARY KEY | `(organization_id)`。UUID正準表現 | CONSTRAINT_VIOLATION |
| UQ-003-01 | UNIQUE | `(organization_code)`。§4.12 IDX-008（無条件UNIQUE）参照 | DUPLICATED（組織コード） |
| FK-003-01 | FOREIGN KEY | `(parent_organization_id) REFERENCES "ORGANIZATION"(organization_id)`、ON DELETE RESTRICT。NULL可 | CONSTRAINT_VIOLATION |
| CK-003-01 | CHECK | UUID形式（`organization_id`、および`parent_organization_id IS NULL OR ...`）。`length(列) = 36`・8-4-4-4-12位置のハイフン・英小文字16進（§4.12.1） | CONSTRAINT_VIOLATION |
| CK-003-02 | CHECK | 文字列長（`length(organization_code) BETWEEN 1 AND 30`、`length(organization_name) BETWEEN 1 AND 200`）（§4.12.1） | CONSTRAINT_VIOLATION |
| CK-003-03 | CHECK | 日付形式（`effective_from`、`effective_to IS NULL OR ...`）。`length(列) = 10`・`YYYY-MM-DD`の桁区切り・`strftime('%Y-%m-%d', 列) = 列`（§4.12.1） | CONSTRAINT_VIOLATION |
| CK-003-04 | CHECK | 期間順序（`effective_to IS NULL OR effective_from <= effective_to`。ISO日付`TEXT`の文字列比較）（§4.12.1） | CONSTRAINT_VIOLATION |
| CK-003-05 | CHECK | 真偽値（`active IN (0, 1)`）（§4.12.1） | CONSTRAINT_VIOLATION |
| TRG-003 | TRIGGER | ORGANIZATION INSERT/UPDATE（§4.12.4、物理名`trg_organization_reference_bi`/`trg_organization_reference_bu`）。自組織参照禁止、再帰CTEで階層循環禁止、親が`active = 1`・未削除で子の有効期間全体を包含、有効な子を孤児化しない、変更後条件から外れる現在・将来所属参照なしを再検証する。違反時は`RAISE(ABORT, 'ORGANIZATION_REFERENCE_INVALID')` | CONSTRAINT_VIOLATION（`ORGANIZATION_REFERENCE_INVALID`。上位でORGANIZATION_HIERARCHY_CONFLICT／MASTER_PERIOD_INVALIDへ写像） |

## 4.6 TBL-004 `POSITION`

### 4.6.1 基本情報

| 項目 | 内容 |
|---|---|
| TBL-ID / 論理名 | TBL-004 / 役職 |
| 目的 / 種別 | 役職・役職レベル・有効期間を保持する / マスター |
| DDL | §4.16 MIG-001でCHECK・UNIQUE・外部キー付きの`STRICT`型付きtableとして作成し、インデックス・トリガーは§4.12（MIG-002）に従う |
| 更新主体 | M-006/IF-24(SQL-024 登録)・IF-25(SQL-025 更新) |
| 保持・削除 | §4.14の論理削除(deleted_at)またはactive=0による無効化。物理削除しない |

### 4.6.2 カラム定義

| カラム | 論理名 | SQLite型 | NULL | DEFAULT | 制約・保存形式 |
|---|---|---|---|---|---|
| position_id | 役職ID | TEXT | 不可 | なし | PK。UUID正準表現 |
| position_code | 役職コード | TEXT | 不可 | なし | 最大30文字、UNIQUE |
| position_name | 役職名 | TEXT | 不可 | なし | 最大200文字 |
| position_level | 役職レベル | INTEGER | 不可 | なし | 0以上 |
| effective_from | 有効開始日 | TEXT | 不可 | なし | `YYYY-MM-DD` |
| effective_to | 有効終了日 | TEXT | 可 | なし | `YYYY-MM-DD`。期限なしはNULL |
| active | 手動利用可否 | INTEGER | 不可 | なし | `CHECK (active IN (0, 1))`。有効期間と独立。1でも未来開始前・期限到来後は指定日利用不可、DISABLE時は即時0 |

### 4.6.3 キー・参照・不変条件

| 制約ID | 種別 | 対象・条件 | 違反時のM-006論理結果 |
|---|---|---|---|
| PK-004-01 | PRIMARY KEY | `(position_id)`。UUID正準表現の主キー | CONSTRAINT_VIOLATION（§7.10.5共通例外） |
| UQ-004-01 | UNIQUE | `(position_code)`（IDX-010参照、無条件UNIQUE） | DUPLICATED（役職コード。上位はCODE_DUPLICATED。§4.13 TX-007／§7.10.5） |
| CK-004-01 | CHECK | `position_id`のUUID形式（`length(position_id) = 36`、8-4-4-4-12ハイフン、英小文字16進）（§4.12.1） | CONSTRAINT_VIOLATION（§7.10.5） |
| CK-004-02 | CHECK | `length(position_code) BETWEEN 1 AND 30`（§4.12.1） | CONSTRAINT_VIOLATION（§7.10.5） |
| CK-004-03 | CHECK | `length(position_name) BETWEEN 1 AND 200`（§4.12.1） | CONSTRAINT_VIOLATION（§7.10.5） |
| CK-004-04 | CHECK | `position_level >= 0`（§4.12.1） | CONSTRAINT_VIOLATION（§7.10.5） |
| CK-004-05 | CHECK | `active IN (0, 1)`（§4.12.1） | CONSTRAINT_VIOLATION（§7.10.5） |
| CK-004-06 | CHECK | `effective_from`・`effective_to`の日付形式（`length(列) = 10`、`YYYY-MM-DD`、`strftime('%Y-%m-%d', 列) = 列`。`effective_to`はNULL許可）（§4.12.1） | CONSTRAINT_VIOLATION（§7.10.5） |
| CK-004-07 | CHECK | 期間順 `effective_to IS NULL OR effective_from <= effective_to`（§4.12.1） | CONSTRAINT_VIOLATION（§7.10.5） |
| CK-004-08 | CHECK | `version >= 1`（§4.14共通カラム・§4.12.1） | CONSTRAINT_VIOLATION（§7.10.5） |
| CK-004-09 | CHECK | `created_at`・`updated_at`・`deleted_at`のUTC ISO 8601日時形式（`deleted_at`はNULL許可）（§4.14共通カラム・§4.12.1） | CONSTRAINT_VIOLATION（§7.10.5） |
| TRG-004 | TRIGGER | `trg_position_reference_bu`（§4.12.4）。POSITION UPDATE時、無効化・期間短縮後の条件から外れる未削除EMPLOYEE_ASSIGNMENT参照がないことを再検証し、違反時 `RAISE(ABORT, 'POSITION_REFERENCE_INVALID')` | POSITION_REFERENCE_INVALID → CONFLICT系業務例外へ写像（§7.10.5） |

POSITIONはFOREIGN KEYを持たない（親参照・自己参照列なし。EMPLOYEE_ASSIGNMENT.position_idの参照先テーブルである）。

#### 期間・利用可否整合性

- `active`は有効期間と独立した手動利用可否とし、指定日利用可否は`active = 1 AND effective_from <= 指定日 AND (effective_to IS NULL OR 指定日 <= effective_to)`で判定する（§4.1）。有効役職一覧（SQL-021）はIDX-011（`POSITION(active, effective_from, effective_to)`）で取得する。
- 役職の無効化（active=0）・期間短縮・論理削除は、現在・将来の未削除所属参照期間を外す場合にTRG-004（`POSITION_REFERENCE_INVALID`）が拒否する（§4.4.2期間整合性・§4.12.4）。

## 4.7 TBL-005 `EMPLOYEE_CHANGE_HISTORY`

### 4.7.1 基本情報

| 項目 | 内容 |
|---|---|
| TBL-ID / 論理名 | TBL-005 / 社員変更履歴 |
| 目的 / 種別 | 業務上参照する社員変更概要を保持する / 追記履歴 |
| DDL | §4.16 MIG-001でCHECK・UNIQUE・外部キー付きの`STRICT`型付きtableとして作成し、インデックス・トリガーは§4.12（MIG-002）に従う |
| 更新主体 | M-006の全合成公開IF(IF-05/08/11/13/34/35)が内部構成SQL SQL-007で追記する |
| 保持・削除 | 追記専用。業務更新・削除を行わない |

### 4.7.2 カラム定義

| カラム | 論理名 | SQLite型 | NULL | DEFAULT | 制約・保存形式 |
|---|---|---|---|---|---|
| history_id | 変更履歴ID | TEXT | 不可 | なし | PK。UUID正準表現 |
| employee_id | 対象社員ID | TEXT | 不可 | なし | FK → EMPLOYEE |
| change_type | 変更種別 | TEXT | 不可 | なし | 最大30文字。`REGISTER` / `BASIC_UPDATE` / `ASSIGNMENT` / `RETIREMENT_SCHEDULED` / `RETIREMENT_CONFIRMED` / `ROLE_UPDATE` |
| change_summary | 変更概要 | TEXT | 不可 | なし | `CHECK (json_valid(change_summary) AND json_type(change_summary) = 'object')`。07のschemaVersion=1契約。`schemaVersion=1`と`changes[]`（`fieldCode`,`operation`だけ）を保持し、変更前後の実値・個人情報・自由記述は保存しない |
| changed_at | 変更日時 | TEXT | 不可 | なし | UTCのISO 8601日時 |
| changed_by | 変更者ID | TEXT | 可 | なし | FK → USER_ACCOUNT。JOB実行時はNULL |
| source_type | 実行元種別 | TEXT | 不可 | なし | 最大20文字。`API` / `JOB` |
| source_id | 実行元ID | TEXT | 不可 | なし | 最大50文字。API-IDまたはJOB-ID |
| trace_id | 相関ID | TEXT | 不可 | なし | 最大100文字。API/JOBのログ相関ID |

### 4.7.3 キー・参照・不変条件

| 制約ID | 種別 | 対象・条件 | 違反時のM-006論理結果 |
|---|---|---|---|
| PK-005-01 | PRIMARY KEY | `(history_id)`。M-006採番のUUID。主キー値の更新禁止 | CONSTRAINT_VIOLATION（SQL-007はbatch内実行のためUUID衝突時はbatch全体ロールバック。採番済のため実質発生しない） |
| FK-005-01 | FOREIGN KEY | `(employee_id) REFERENCES EMPLOYEE(employee_id)`、ON DELETE RESTRICT | CONSTRAINT_VIOLATION（対象社員不在時はbatch全体ロールバック） |
| FK-005-02 | FOREIGN KEY | `(changed_by) REFERENCES USER_ACCOUNT(user_id)`、ON DELETE RESTRICT。NULL可（JOB実行時） | CONSTRAINT_VIOLATION（変更者不在時はbatch全体ロールバック） |
| CK-005-01 | CHECK | history_id・employee_id・changed_byのUUID形式（`length(列) = 36`、8-4-4-4-12位置のハイフン、英小文字16進。changed_byは`列 IS NULL OR ...`）（§4.12.1） | CONSTRAINT_VIOLATION |
| CK-005-02 | CHECK | `change_type IN ('REGISTER', 'BASIC_UPDATE', 'ASSIGNMENT', 'RETIREMENT_SCHEDULED', 'RETIREMENT_CONFIRMED', 'ROLE_UPDATE')`（§4.12.1） | CONSTRAINT_VIOLATION |
| CK-005-03 | CHECK | `source_type IN ('API', 'JOB')`（§4.12.1） | CONSTRAINT_VIOLATION |
| CK-005-04 | CHECK | `json_valid(change_summary) AND json_type(change_summary) = 'object'`（§4.12.1） | CONSTRAINT_VIOLATION |
| CK-005-05 | CHECK | changed_atのUTC `YYYY-MM-DDTHH:mm:ss.SSSZ`日時形式（§4.12.1） | CONSTRAINT_VIOLATION |
| CK-005-06 | CHECK | 最大長 `length(列) BETWEEN 1 AND N`（change_type=30、source_type=20、source_id=50、trace_id=100）（§4.12.1） | CONSTRAINT_VIOLATION |

- TRIGGER: EMPLOYEE_CHANGE_HISTORYは追記専用で期間重複・業務整合性triggerの対象に含まれず、§4.12.3・§4.12.4のTRG-001〜005を新設・付与しない。
- IDX-012（`employee_id, changed_at DESC, history_id DESC`）は一意でない参照用INDEXのため一意制約（不変条件）として扱わない（§4.12）。

## 4.8 TBL-006 `USER_ACCOUNT`

### 4.8.1 基本情報

| 項目 | 内容 |
|---|---|
| TBL-ID / 論理名 | TBL-006 / 利用者アカウント |
| 目的 / 種別 | 外部認証主体と社員を対応付け、システム利用状態を保持する / マスター |
| DDL | §4.16 MIG-001でCHECK・UNIQUE・外部キー付きの`STRICT`型付きtableとして作成し、インデックス・トリガーは§4.12（MIG-002）に従う |
| 更新主体 | M-006/IF-35(SQL-035 版数・ロール割当更新の所有者) |
| 保持・削除 | §4.14の論理削除(deleted_at)。物理削除しない |

### 4.8.2 カラム定義

| カラム | 論理名 | SQLite型 | NULL | DEFAULT | 制約・保存形式 |
|---|---|---|---|---|---|
| user_id | 利用者ID | TEXT | 不可 | なし | PK。UUID正準表現 |
| employee_id | 社員ID | TEXT | 可 | なし | FK → EMPLOYEE。一意性はIDX-014の部分UNIQUE（有効な社員行のみ）で保証する。社員外管理者はNULL可 |
| identity_subject | 外部認証主体 | TEXT | 不可 | なし | 最大255文字、UNIQUE |
| active | 利用状態 | INTEGER | 不可 | なし | `CHECK (active IN (0, 1))`。0の利用者はログイン不可 |

### 4.8.3 キー・参照・不変条件

| 制約ID | 種別 | 対象・条件 | 違反時のM-006論理結果 |
|---|---|---|---|
| PK-006-01 | PRIMARY KEY | `(user_id)`。UUID正準表現 | CONSTRAINT_VIOLATION（§7.10.5 CHECK・FOREIGN KEY・trigger ABORT系） |
| UQ-006-01 | UNIQUE | `(identity_subject)`（IDX-013参照。外部認証主体の一意性。§4.12で二重定義しない） | DUPLICATED（§7.10.5 UNIQUE違反） |
| UQ-006-02 | UNIQUE | `(employee_id) WHERE employee_id IS NOT NULL AND deleted_at IS NULL`（IDX-014参照。有効な社員と利用者の1対0..1の部分UNIQUE） | DUPLICATED（§7.10.5 UNIQUE違反） |
| FK-006-01 | FOREIGN KEY | `(employee_id) REFERENCES EMPLOYEE(employee_id)`、ON DELETE RESTRICT（§4.12）。社員外管理者はNULL可 | CONSTRAINT_VIOLATION（§7.10.5 FOREIGN KEY ABORT） |
| CK-006-01 | CHECK | user_id は`length(user_id) = 36`・8-4-4-4-12位置ハイフン・英小文字16進のUUID形式（§4.12.1） | CONSTRAINT_VIOLATION（§7.10.5 CHECK ABORT） |
| CK-006-02 | CHECK | `employee_id IS NULL OR`（`length(employee_id) = 36`・8-4-4-4-12位置ハイフン・英小文字16進のUUID形式）（§4.12.1） | CONSTRAINT_VIOLATION（§7.10.5 CHECK ABORT） |
| CK-006-03 | CHECK | `length(identity_subject) BETWEEN 1 AND 255`（§4.12・§4.12.1） | CONSTRAINT_VIOLATION（§7.10.5 CHECK ABORT） |
| CK-006-04 | CHECK | `active IN (0, 1)`（真偽列。§4.12.1） | CONSTRAINT_VIOLATION（§7.10.5 CHECK ABORT） |
| TRG-005 | TRIGGER | USER_ROLE INSERT/UPDATE時に、参照先USER_ACCOUNTが未削除かつ`active = 1`であることを最終防衛検証する（§4.12.4。トリガーはUSER_ROLE側で発火し、新設しない） | USER_ROLE_REFERENCE_INVALID → CONSTRAINT_VIOLATION（§4.12.4／§7.10.5 trigger ABORT） |

- version・created_at・updated_at・deleted_at等の共通カラムは§4.14の更新可能テーブル共通に従い、`CHECK (version >= 1)`と`deleted_at IS NULL`を有効行条件とする。
- UQ-006-02・IDX-014の部分UNIQUEは`deleted_at IS NULL`の有効行だけを一意対象とし、論理削除済み社員対応行は一意判定から除外する。

## 4.9 TBL-007 `ROLE`

### 4.9.1 基本情報

| 項目 | 内容 |
|---|---|
| TBL-ID / 論理名 | TBL-007 / ロール |
| 目的 / 種別 | システム上の役割を保持する / マスター |
| DDL | §4.16 MIG-001でCHECK・UNIQUE・外部キー付きの`STRICT`型付きtableとして作成し、インデックス・トリガーは§4.12（MIG-002）に従う |
| 更新主体 | 業務機能からの更新IFは持たない。固定4件を§4.16 MIG-002で冪等投入する |
| 保持・削除 | §4.14の論理削除(deleted_at)またはactive=0による無効化。アプリ機能から追加・変更・削除しない(§2.4) |

### 4.9.2 カラム定義

| カラム | 論理名 | SQLite型 | NULL | DEFAULT | 制約・保存形式 |
|---|---|---|---|---|---|
| role_id | ロールID | TEXT | 不可 | なし | PK。UUID正準表現 |
| role_code | ロールコード | TEXT | 不可 | なし | 最大30文字、UNIQUE。§4.9.1の4コードだけを許可 |
| role_name | ロール名 | TEXT | 不可 | なし | 最大100文字。§4.9.1の固定名称。アプリケーションから変更しない |
| active | 利用状態 | INTEGER | 不可 | なし | `CHECK (active IN (0, 1))`。0のロールは新規割当不可 |

### 4.9.1 固定ロール初期データ

ROLEはアプリケーション管理対象の可変マスターではなく、配置時マイグレーションで次の4件を冪等に初期投入する。アプリケーションAPI・JOB・モジュールから`role_code`と`role_name`の登録・更新・削除を行わない。利用停止が必要な場合の`active`変更も承認済み運用マイグレーションで行う。

| role_code | role_name | 初期active |
|---|---|---|
| `HR` | 人事担当者 | 1 |
| `DEPARTMENT_MANAGER` | 部門管理者 | 1 |
| `EMPLOYEE` | 一般社員 | 1 |
| `SYSTEM_ADMIN` | システム管理者 | 1 |

### 4.9.3 キー・参照・不変条件

| 制約ID | 種別 | 対象・条件 | 違反時のM-006論理結果 |
|---|---|---|---|
| PK-007-01 | PRIMARY KEY | `(role_id)`。UUID正準表現。§4.12の削除RESTRICT・主キー値更新禁止方針に従う | CONSTRAINT_VIOLATION（§7.10.5）。ROLEは§4.9.1・§4.16 MIG-001/MIG-002の冪等投入だけで更新され実行時M-006の書込経路を持たないため、実行時には発生しない |
| UQ-007-01 | UNIQUE | `(role_code)`。§4.12 IDX-015（UNIQUE・無条件）を参照。§4.12.1の`role_code IN`列挙と併せ§4.9.1の4コードへ限定 | DUPLICATED（§7.10.5 UNIQUE違反）。上記PK-007-01と同じくMIG投入時だけの防衛で実行時M-006書込経路はない |
| CK-007-01 | CHECK | `role_code IN ('HR', 'DEPARTMENT_MANAGER', 'EMPLOYEE', 'SYSTEM_ADMIN')`（§4.12.1） | CONSTRAINT_VIOLATION（§7.10.5 CHECK違反） |
| CK-007-02 | CHECK | `active IN (0, 1)`（§4.12.1 真偽列） | CONSTRAINT_VIOLATION（§7.10.5 CHECK違反） |

- role_id・role_code・role_nameの`length()`・UUID形式・空文字・最大長CHECKは§4.12.1の共通形式CHECKとして各列の§4.9.2「制約・保存形式」に保持し、本表では列挙・範囲・一意の名前付き不変条件だけをID化する。
- ROLEは外部キーを持たず他表を参照しない。ROLEを参照する外部キーは割当側のUSER_ROLE(role_id) → ROLE(role_id)（削除RESTRICT、§4.12）であり、その不変条件はUSER_ROLE側で扱う。
- ROLE自身に発火するTRIGGERはない。ROLEの`active = 1`・未削除は、割当側USER_ROLEのINSERT/UPDATE時にTRG-005（§4.12.4、安定コード`USER_ROLE_REFERENCE_INVALID`）が再検証し、有効な現行割当の重複防止はIDX-016（§4.12）が担う。

## 4.10 TBL-008 `USER_ROLE`

### 4.10.1 基本情報

| 項目 | 内容 |
|---|---|
| TBL-ID / 論理名 | TBL-008 / 利用者ロール |
| 目的 / 種別 | 利用者へのロール割当と有効期間を保持する / 有効期間履歴 |
| DDL | §4.16 MIG-001でCHECK・UNIQUE・外部キー付きの`STRICT`型付きtableとして作成し、インデックス・トリガーは§4.12（MIG-002）に従う |
| 更新主体 | M-006/IF-35(内部構成SQL SQL-029 現行終了・SQL-030 新規登録) |
| 保持・削除 | §4.14の論理削除(deleted_at)。終了はeffective_to、将来割当の取消はdeleted_atで表す |

### 4.10.2 カラム定義

| カラム | 論理名 | SQLite型 | NULL | DEFAULT | 制約・保存形式 |
|---|---|---|---|---|---|
| user_role_id | 利用者ロールID | TEXT | 不可 | なし | PK。UUID正準表現 |
| user_id | 利用者ID | TEXT | 不可 | なし | FK → USER_ACCOUNT |
| role_id | ロールID | TEXT | 不可 | なし | FK → ROLE |
| effective_from | 有効開始日 | TEXT | 不可 | なし | `YYYY-MM-DD` |
| effective_to | 有効終了日 | TEXT | 可 | なし | `YYYY-MM-DD`。現行割当はNULL |

### 4.10.3 キー・参照・不変条件

| 制約ID | 種別 | 対象・条件 | 違反時のM-006論理結果 |
|---|---|---|---|
| PK-008-01 | PRIMARY KEY | `(user_role_id)`。主キー値の更新禁止（§4.12） | CONSTRAINT_VIOLATION（M-006生成UUIDのため業務重複例外化しない） |
| UQ-008-01（IDX-016参照） | UNIQUE | `USER_ROLE(user_id, role_id) WHERE effective_to IS NULL AND deleted_at IS NULL`（IDX-016 PARTIAL UNIQUE。有効な現行ロールの重複割当防止） | DUPLICATED（重複ロール割当） |
| FK-008-01 | FOREIGN KEY | `(user_id) REFERENCES USER_ACCOUNT(user_id)`、ON DELETE RESTRICT（§4.12） | CONSTRAINT_VIOLATION |
| FK-008-02 | FOREIGN KEY | `(role_id) REFERENCES ROLE(role_id)`、ON DELETE RESTRICT（§4.12） | CONSTRAINT_VIOLATION |
| CK-008-01 | CHECK | `user_role_id`・`user_id`・`role_id`のUUID形式（§4.12.1：`length = 36`・8-4-4-4-12ハイフン・英小文字16進） | CONSTRAINT_VIOLATION |
| CK-008-02 | CHECK | `effective_from`・`effective_to`の日付形式（§4.12.1：`length = 10`・`YYYY-MM-DD`・`strftime('%Y-%m-%d', 列) = 列`。`effective_to`はNULL許可） | CONSTRAINT_VIOLATION |
| CK-008-03 | CHECK | `effective_to`がある場合に`effective_from <= effective_to`（§4.12.1、ISO日付TEXTの文字列比較） | CONSTRAINT_VIOLATION |
| CK-008-04 | CHECK | `CHECK (version >= 1)`（§4.14共通カラム。初期値1・更新成功時に1加算） | CONSTRAINT_VIOLATION |
| CK-008-05 | CHECK | `created_at`・`updated_at`・`deleted_at`の日時形式（§4.12.1：UTCの`YYYY-MM-DDTHH:mm:ss.SSSZ`。`deleted_at`はNULL許可） | CONSTRAINT_VIOLATION |
| trg_user_role_period_bi/_bu | TRIGGER | USER_ROLE INSERT/UPDATEの`(user_id, role_id)`期間重複防止（§4.12.3、終了日NULLを無限未来として両端包含判定）→ `RAISE(ABORT, 'USER_ROLE_PERIOD_OVERLAP')` | ROLE_ASSIGNMENT_PERIOD_CONFLICT（§4.13 TX-008の期間重複公開例外へ写像） |
| TRG-005 | TRIGGER | USER_ROLE INSERT/UPDATEでROLEとUSER_ACCOUNTが未削除、かつROLEが`active = 1`（§4.12.4、trg_user_role_reference_bi/_bu）→ `RAISE(ABORT, 'USER_ROLE_REFERENCE_INVALID')` | CONSTRAINT_VIOLATION（§7.10.4のROLE_NOT_ACTIVE等CONFLICT系業務条件へ写像） |

## 4.11 TBL-009 `AUDIT_LOG`

### 4.11.1 基本情報

| 項目 | 内容 |
|---|---|
| TBL-ID / 論理名 | TBL-009 / 監査ログ |
| 目的 / 種別 | 操作証跡とログ相関情報を保持する / 追記ログ |
| DDL | §4.16 MIG-001でCHECK・UNIQUE・外部キー付きの`STRICT`型付きtableとして作成し、インデックス・トリガーは§4.12（MIG-002）に従う |
| 更新主体 | M-006/IF-31(SQL-031 監査ログ登録。業務更新成功後の独立`run()`) |
| 保持・削除 | 追記専用。業務更新・削除を行わない |

### 4.11.2 カラム定義

| カラム | 論理名 | SQLite型 | NULL | DEFAULT | 制約・保存形式 |
|---|---|---|---|---|---|
| audit_id | 監査ログID | TEXT | 不可 | なし | PK。UUID正準表現 |
| user_id | 操作者ID | TEXT | 可 | なし | FK → USER_ACCOUNT。JOB実行時、またはAUTHの認証前失敗で主体未特定時はNULL |
| action | 操作 | TEXT | 不可 | なし | 最大50文字。監査対象の論理操作 |
| target_type | 対象種別 | TEXT | 不可 | なし | 最大50文字。社員 / 組織 / 役職 / 権限 等 |
| target_id | 対象ID | TEXT | 可 | なし | UUID正準表現。対象が特定できない失敗時、および`EMPLOYEE_EXPORT`等の単一対象を持たない集合操作の成功時はNULL |
| occurred_at | 発生日時 | TEXT | 不可 | なし | UTCのISO 8601日時 |
| result | 結果 | TEXT | 不可 | なし | 最大20文字。`SUCCESS` / `FAILURE` |
| trace_id | 相関ID | TEXT | 不可 | なし | 最大100文字。API/JOB/AUTHのログ相関ID |
| source_type | 実行元種別 | TEXT | 不可 | なし | 最大20文字。`API` / `JOB` / `AUTH` |
| source_id | 実行元ID | TEXT | 不可 | なし | 最大50文字。API-ID、JOB-ID、または認証処理ID |
| detail_summary | 詳細概要 | TEXT | 不可 | なし | `CHECK (json_valid(detail_summary) AND json_type(detail_summary) = 'object')`。個人情報の全内容を含めず、条件・件数・エラー分類を保持。AUTH失敗は理由分類だけを保持する |

### 4.11.3 キー・参照・不変条件

AUDIT_LOGは追記専用ログのため一意制約(UNIQUE)とトリガーを持たず、§4.12のIDX-018・IDX-019は非UNIQUEの検索用INDEXである。列値はM-006がSQL-031で正規化・生成した確定値のみをバインドするため、下表の制約違反は通常到達不能であり、発生時はM-006が§7.10.5のD1例外変換で全公開IF共通のCONSTRAINT_VIOLATIONへ写像する。IF-31(SQL-031 独立`run()`)の固有論理例外はDATA_ACCESS_ERRORだけとする(§7.10.4)。

| 制約ID | 種別 | 対象・条件 | 違反時のM-006論理結果 |
|---|---|---|---|
| PK-009-01 | PRIMARY KEY | `(audit_id)` | CONSTRAINT_VIOLATION |
| FK-009-01 | FOREIGN KEY | `(user_id) REFERENCES USER_ACCOUNT(user_id)`、ON DELETE RESTRICT | CONSTRAINT_VIOLATION |
| CK-009-01 | CHECK | audit_id・user_id・target_idのUUID形式（`length(列) = 36`・8-4-4-4-12位置のハイフン・英小文字16進。NULL可のuser_id・target_idはNULLを許容）(§4.12.1) | CONSTRAINT_VIOLATION |
| CK-009-02 | CHECK | occurred_atの日時形式（UTCの`YYYY-MM-DDTHH:mm:ss.SSSZ`）(§4.12.1) | CONSTRAINT_VIOLATION |
| CK-009-03 | CHECK | `result IN ('SUCCESS', 'FAILURE')`(§4.12.1) | CONSTRAINT_VIOLATION |
| CK-009-04 | CHECK | `source_type IN ('API', 'JOB', 'AUTH')`(§4.12.1) | CONSTRAINT_VIOLATION |
| CK-009-05 | CHECK | `json_valid(detail_summary) AND json_type(detail_summary) = 'object'`(§4.12.1) | CONSTRAINT_VIOLATION |
| CK-009-06 | CHECK | action・target_type・result・trace_id・source_type・source_idの`length(列) BETWEEN 1 AND 最大長`（各50/50/20/100/20/50文字）(§4.12) | CONSTRAINT_VIOLATION |

## 4.12 制約・インデックス

| ID | 対象 | 種別 | 定義・目的 | 対応SQL-ID |
|---|---|---|---|---|
| IDX-001 | EMPLOYEE(employee_number) | UNIQUE | 社員番号の一意性 | SQL-001, SQL-002, SQL-004, SQL-005, SQL-032 |
| IDX-002 | EMPLOYEE(lower(email)) | UNIQUE | メールアドレスの大文字小文字非依存一意性 | SQL-004, SQL-005, SQL-008 |
| IDX-003 | EMPLOYEE(retirement_date, employee_id) WHERE status = 'ACTIVE' AND retirement_date IS NOT NULL AND deleted_at IS NULL | PARTIAL INDEX | JOB-001の未反映到来退職対象をキーセット順で取得する | SQL-012 |
| IDX-004 | EMPLOYEE(last_name, first_name, employee_id) | INDEX | 氏名順の安定ソート（将来の氏名順一覧向け。現行§8 SQLは社員番号順で整列するため本索引は未使用）。任意位置の部分一致は`instr()`評価のため本索引を前提としない | なし（現行未使用・将来の氏名順一覧向け） |
| IDX-005 | EMPLOYEE_ASSIGNMENT(employee_id) WHERE effective_to IS NULL AND deleted_at IS NULL | PARTIAL UNIQUE | 有効な現行所属を社員ごとに1件へ制約 | SQL-006 |
| IDX-006 | EMPLOYEE_ASSIGNMENT(employee_id, effective_from DESC) | INDEX | 所属履歴・現所属取得 | SQL-001, SQL-002, SQL-003, SQL-009, SQL-010, SQL-026, SQL-032 |
| IDX-007 | EMPLOYEE_ASSIGNMENT(organization_id, effective_from, effective_to, employee_id) WHERE deleted_at IS NULL | PARTIAL INDEX | 基準日時点の組織別社員検索 | SQL-001, SQL-002, SQL-017, SQL-032 |
| IDX-008 | ORGANIZATION(organization_code) | UNIQUE | 組織コードの一意性 | SQL-016, SQL-018, SQL-019 |
| IDX-009 | ORGANIZATION(active, effective_from, effective_to) | INDEX | 有効組織一覧 | SQL-016 |
| IDX-010 | POSITION(position_code) | UNIQUE | 役職コードの一意性 | SQL-023, SQL-024 |
| IDX-011 | POSITION(active, effective_from, effective_to) | INDEX | 有効役職一覧 | SQL-021 |
| IDX-012 | EMPLOYEE_CHANGE_HISTORY(employee_id, changed_at DESC, history_id DESC) | INDEX | 変更履歴一覧 | SQL-014, SQL-015 |
| IDX-013 | USER_ACCOUNT(identity_subject) | UNIQUE | 外部認証主体の一意性 | SQL-026 |
| IDX-014 | USER_ACCOUNT(employee_id) WHERE employee_id IS NOT NULL AND deleted_at IS NULL | PARTIAL UNIQUE | 有効な社員と利用者の1対0..1 | SQL-033 |
| IDX-015 | ROLE(role_code) | UNIQUE | ロールコードの一意性 | SQL-027 |
| IDX-016 | USER_ROLE(user_id, role_id) WHERE effective_to IS NULL AND deleted_at IS NULL | PARTIAL UNIQUE | 有効な現行ロールの重複割当防止 | SQL-030（一意制約の最終防衛） |
| IDX-017 | USER_ROLE(user_id, effective_to, effective_from) WHERE deleted_at IS NULL | PARTIAL INDEX | 現行・期間指定ロール取得 | SQL-026, SQL-028, SQL-029, SQL-030 |
| IDX-018 | AUDIT_LOG(occurred_at DESC, audit_id DESC) | INDEX | 期間監査検索（将来の監査参照UI向け。現行§8 SQLはAUDIT_LOGへSQL-031のINSERTのみで本索引は未使用） | なし（現行未使用・将来の監査参照UI向け） |
| IDX-019 | AUDIT_LOG(target_type, target_id, occurred_at DESC) | INDEX | 対象別監査検索（将来の監査参照UI向け。現行§8 SQLはAUDIT_LOGへSQL-031のINSERTのみで本索引は未使用） | なし（現行未使用・将来の監査参照UI向け） |
| IDX-020 | EMPLOYEE_ASSIGNMENT(manager_employee_id, effective_from, effective_to, employee_id) WHERE manager_employee_id IS NOT NULL AND deleted_at IS NULL | PARTIAL INDEX | 退職時の上長逆参照と将来参照検証 | SQL-009 |
| IDX-021 | EMPLOYEE_ASSIGNMENT(position_id, effective_from, effective_to, employee_id) WHERE deleted_at IS NULL | PARTIAL INDEX | 役職の無効化・期間短縮時の参照検証 | SQL-001, SQL-002, SQL-022, SQL-032 |
| IDX-022 | ORGANIZATION(parent_organization_id) WHERE deleted_at IS NULL | PARTIAL INDEX | 階層循環・子組織包含検証 | TRG-003の階層循環・子組織包含検証で使用（利用SQLなし） |

- 部分インデックスを利用するSQLは、対象インデックスと同じ論理削除・状態条件を検索条件へ含める。
- SQLiteの`TEXT`長は型名では制限されないため、最大長を持つ列はすべて`CHECK (length(列) BETWEEN 1 AND 最大長)`を持つ。NULL可の列は`列 IS NULL OR ...`とする。
- 外部キーはマイグレーションで明示し、D1で有効な外部キー検証を利用する。削除は`RESTRICT`、主キー値の更新は禁止する。
- IDX-001（employee_number）・IDX-002（lower(email)）・IDX-008（organization_code）・IDX-010（position_code）の対象表EMPLOYEE・ORGANIZATION・POSITIONは、現行§8 SQLで`deleted_at`を設定する更新（SQL-010・SQL-029はEMPLOYEE_ASSIGNMENT・USER_ROLEのみ対象）を持たず論理削除行を生成しないため無条件UNIQUEとする。SQL-004・SQL-018・SQL-023等の事前確認が持つ`deleted_at IS NULL`は将来の論理削除導入に備えた冗長条件であり、導入時はIDX-005・IDX-014・IDX-016と同じ部分UNIQUEへ揃える。

### 4.12.1 型・チェック制約

- EMPLOYEE〜AUDIT_LOGおよびTX_MUTATION_GUARDの物理DDLはすべて末尾を`) STRICT;`とする。SQLiteの`INTEGER` / `REAL` / `TEXT` / `BLOB`以外を本システムの物理型に使用せず、FTS5 virtual tableだけを例外とする。
- 以下のUUID・日付・日時の形式CHECKはEMPLOYEE〜AUDIT_LOGの業務列に適用する。技術テーブルTX_MUTATION_GUARDはM-006が生成する値だけを保持するため、STRICTと§4.13.1のDDLに明示したCHECK（`owner_type`・`operation_type`の列挙、`json_valid()`とJSON型、所有者・operationの整合）だけを持ち、`guard_id`・`pivot_date`・`created_at`へ形式CHECKを課さない。
- UUIDを保持する`TEXT`列は、`length(列) = 36`、8-4-4-4-12位置のハイフン、英小文字の16進文字だけを許可するCHECKを持つ。
- 日付`TEXT`列は`length(列) = 10`、`YYYY-MM-DD`の桁・区切り、および`strftime('%Y-%m-%d', 列) = 列`を満たすCHECKを持つ。NULL可の列はNULLを許可する。
- 日時`TEXT`列はUTCの`YYYY-MM-DDTHH:mm:ss.SSSZ`を満たすCHECKを持つ。M-006は同じ形式へ正規化した値だけをバインドする。
- 真偽列は`INTEGER CHECK (列 IN (0, 1))`とする。
- JSON列は`TEXT CHECK (json_valid(列))`を必須とし、change_summary・detail_summaryは`json_type(列) = 'object'`、更新ガードのID集合は`json_type(列) = 'array'`を追加する。
- EMPLOYEEは`retirement_date IS NULL OR hire_date <= retirement_date`を満たす。
- EMPLOYEEの`status = 'RETIRED'`では`retirement_date IS NOT NULL`を満たす。
- EMPLOYEEの`status`は`ACTIVE`または`RETIRED`のいずれかを満たす。
- EMPLOYEEは`employment_type_code IN ('REGULAR', 'CONTRACT', 'PART_TIME', 'TEMPORARY', 'OTHER')`を満たす。
- EMPLOYEEは`retirement_type_code IS NULL OR retirement_type_code IN ('VOLUNTARY', 'COMPANY', 'RETIREMENT_AGE', 'CONTRACT_END', 'OTHER')`を満たす。
- 各有効期間は終了日がある場合に`effective_from <= effective_to`を満たす。ISO日付`TEXT`のため文字列比較を使用する。
- EMPLOYEE_ASSIGNMENTは`manager_employee_id IS NULL OR manager_employee_id <> employee_id`を満たす。
- POSITIONの`position_level`は0以上とする。
- ROLEは`role_code IN ('HR', 'DEPARTMENT_MANAGER', 'EMPLOYEE', 'SYSTEM_ADMIN')`を満たす。
- EMPLOYEE_CHANGE_HISTORYの`change_type`は`REGISTER`、`BASIC_UPDATE`、`ASSIGNMENT`、`RETIREMENT_SCHEDULED`、`RETIREMENT_CONFIRMED`、`ROLE_UPDATE`のいずれか、`source_type`は`API`または`JOB`のいずれかとする。
- AUDIT_LOGの`result`は`SUCCESS`または`FAILURE`のいずれか、`source_type`は`API`、`JOB`、`AUTH`のいずれかとする。

### 4.12.2 部分一致検索とFTS5移行候補

初期正本SQLは社員番号と`last_name || ' ' || first_name`に対する`instr(lower(対象), lower(term)) > 0`である。ワイルドカード構文を使用しないため、入力値の`%`、`_`、`\`を検索演算子として解釈しない。D1の通常索引では任意位置の部分一致を高速化できないため、データ量・実測時間が非機能要件の移行基準を超えた場合に限りFTS5へ移行する。

FTS5へ移行する場合は、D1が提供するSQLite FTS5の`trigram` tokenizerを使い、次の任意マイグレーションを適用する。`employee_id`は検索語として索引化しない。この任意のFTS5 virtual table（`EMPLOYEE_SEARCH_FTS`）をFTS-001とする。FTS-001は初期不採用の任意候補のため§4.2のテーブル一覧（対象10表）には含めず、このDDLは初期マイグレーションおよび初期正本SQLにも含めない。採用する場合だけ§4.16のMIG-003で作成する。

```sql
CREATE VIRTUAL TABLE "EMPLOYEE_SEARCH_FTS" USING fts5(
    employee_id UNINDEXED,
    employee_number,
    full_name,
    tokenize = 'trigram case_sensitive 0'
);

CREATE TRIGGER "trg_employee_search_fts_ai"
AFTER INSERT ON "EMPLOYEE"
WHEN NEW.deleted_at IS NULL
BEGIN
    INSERT INTO "EMPLOYEE_SEARCH_FTS" (employee_id, employee_number, full_name)
    VALUES (NEW.employee_id, NEW.employee_number, NEW.last_name || ' ' || NEW.first_name);
END;

CREATE TRIGGER "trg_employee_search_fts_au"
AFTER UPDATE OF employee_number, last_name, first_name, deleted_at ON "EMPLOYEE"
BEGIN
    DELETE FROM "EMPLOYEE_SEARCH_FTS" WHERE employee_id = OLD.employee_id;
    INSERT INTO "EMPLOYEE_SEARCH_FTS" (employee_id, employee_number, full_name)
    SELECT NEW.employee_id, NEW.employee_number, NEW.last_name || ' ' || NEW.first_name
    WHERE NEW.deleted_at IS NULL;
END;

CREATE TRIGGER "trg_employee_search_fts_ad"
AFTER DELETE ON "EMPLOYEE"
BEGIN
    DELETE FROM "EMPLOYEE_SEARCH_FTS" WHERE employee_id = OLD.employee_id;
END;
```

FTS5採用時は正規化後3文字以上をFTS5、1～2文字を初期正本の`instr()`で検索する。切替時は有効なEMPLOYEEから全件投入し、両方式の検索結果一致を検証してからFTS5経路を開放する。本番運用でEMPLOYEEの物理削除は行わないが、マイグレーション・復旧時の索引残留を避けるためDELETE同期トリガーも定義する。

### 4.12.3 期間重複防止トリガー

終了日NULLを無限未来とみなし、両端包含で重複を判定する。D1ではINSERT用とUPDATE用を分け、該当時は`RAISE(ABORT, エラーコード)`で同一バッチ全体を失敗させる。

```sql
CREATE TRIGGER "trg_assignment_period_bi"
BEFORE INSERT ON "EMPLOYEE_ASSIGNMENT"
WHEN NEW.deleted_at IS NULL AND EXISTS (
    SELECT 1
    FROM "EMPLOYEE_ASSIGNMENT" a
    WHERE a.employee_id = NEW.employee_id
      AND a.deleted_at IS NULL
      AND a.effective_from <= coalesce(NEW.effective_to, '9999-12-31')
      AND NEW.effective_from <= coalesce(a.effective_to, '9999-12-31')
)
BEGIN
    SELECT RAISE(ABORT, 'ASSIGNMENT_PERIOD_OVERLAP');
END;

CREATE TRIGGER "trg_assignment_period_bu"
BEFORE UPDATE OF employee_id, effective_from, effective_to, deleted_at
ON "EMPLOYEE_ASSIGNMENT"
WHEN NEW.deleted_at IS NULL AND EXISTS (
    SELECT 1
    FROM "EMPLOYEE_ASSIGNMENT" a
    WHERE a.employee_id = NEW.employee_id
      AND a.assignment_id <> NEW.assignment_id
      AND a.deleted_at IS NULL
      AND a.effective_from <= coalesce(NEW.effective_to, '9999-12-31')
      AND NEW.effective_from <= coalesce(a.effective_to, '9999-12-31')
)
BEGIN
    SELECT RAISE(ABORT, 'ASSIGNMENT_PERIOD_OVERLAP');
END;

CREATE TRIGGER "trg_user_role_period_bi"
BEFORE INSERT ON "USER_ROLE"
WHEN NEW.deleted_at IS NULL AND EXISTS (
    SELECT 1
    FROM "USER_ROLE" ur
    WHERE ur.user_id = NEW.user_id
      AND ur.role_id = NEW.role_id
      AND ur.deleted_at IS NULL
      AND ur.effective_from <= coalesce(NEW.effective_to, '9999-12-31')
      AND NEW.effective_from <= coalesce(ur.effective_to, '9999-12-31')
)
BEGIN
    SELECT RAISE(ABORT, 'USER_ROLE_PERIOD_OVERLAP');
END;

CREATE TRIGGER "trg_user_role_period_bu"
BEFORE UPDATE OF user_id, role_id, effective_from, effective_to, deleted_at
ON "USER_ROLE"
WHEN NEW.deleted_at IS NULL AND EXISTS (
    SELECT 1
    FROM "USER_ROLE" ur
    WHERE ur.user_id = NEW.user_id
      AND ur.role_id = NEW.role_id
      AND ur.user_role_id <> NEW.user_role_id
      AND ur.deleted_at IS NULL
      AND ur.effective_from <= coalesce(NEW.effective_to, '9999-12-31')
      AND NEW.effective_from <= coalesce(ur.effective_to, '9999-12-31')
)
BEGIN
    SELECT RAISE(ABORT, 'USER_ROLE_PERIOD_OVERLAP');
END;
```

### 4.12.4 業務整合性トリガー

アプリケーションでの事前検証に加え、次のトリガーを最終防衛としてマイグレーションに定義する。いずれも違反時は固定エラーコードで`RAISE(ABORT, ...)`し、M-006が業務エラーへ変換する。

| Trigger-ID | タイミング・対象 | 再検証内容 | DBエラーコード |
|---|---|---|---|
| TRG-001 | EMPLOYEE_ASSIGNMENT INSERT/UPDATE | 組織・役職が`active = 1`で所属期間全体を包含すること、上長が本人以外で在籍し、上長退職日の前日までに所属が終了すること | `ASSIGNMENT_REFERENCE_INVALID` |
| TRG-002 | EMPLOYEE UPDATE retirement_date/status | 退職日当日以後に有効な部下からの上長参照がないこと | `MANAGER_REFERENCE_EXISTS` |
| TRG-003 | ORGANIZATION INSERT/UPDATE | 再帰CTEで循環がないこと、親が有効で子期間を包含すること、有効な子を孤児化しないこと、変更後条件から外れる所属参照がないこと | `ORGANIZATION_REFERENCE_INVALID` |
| TRG-004 | POSITION UPDATE | 無効化・期間短縮後の条件から外れる所属参照がないこと | `POSITION_REFERENCE_INVALID` |
| TRG-005 | USER_ROLE INSERT/UPDATE | ROLEとUSER_ACCOUNTが削除されておらず、ROLEが`active = 1`であること | `USER_ROLE_REFERENCE_INVALID` |

TRG-001とTRG-005は論理削除後の行を参照検証対象外とする。TRG-002～004は現在・将来の未削除参照を再検索し、退職、無効化、期間短縮による不整合も拒否する。トリガー内の日付比較は正規化済みISO日付`TEXT`の比較を使用する。DBエラーだけを正常系の分岐手段にはせず、M-002の事前検証結果と一致することを試験する。

次をTRG-001〜005のマイグレーション正本とする。Trigger-IDは論理ID、物理トリガー名の`_bi`/`_bu`はINSERT/UPDATE別実装を表す。

```sql
CREATE TRIGGER "trg_assignment_reference_bi"
BEFORE INSERT ON "EMPLOYEE_ASSIGNMENT"
WHEN NEW.deleted_at IS NULL AND (
    NOT EXISTS (
        SELECT 1 FROM "ORGANIZATION" o
        WHERE o.organization_id = NEW.organization_id
          AND o.active = 1 AND o.deleted_at IS NULL
          AND o.effective_from <= NEW.effective_from
          AND (o.effective_to IS NULL OR (
              NEW.effective_to IS NOT NULL
              AND o.effective_to >= NEW.effective_to
          ))
    )
    OR NOT EXISTS (
        SELECT 1 FROM "POSITION" p
        WHERE p.position_id = NEW.position_id
          AND p.active = 1 AND p.deleted_at IS NULL
          AND p.effective_from <= NEW.effective_from
          AND (p.effective_to IS NULL OR (
              NEW.effective_to IS NOT NULL
              AND p.effective_to >= NEW.effective_to
          ))
    )
    OR (NEW.manager_employee_id IS NOT NULL AND NOT EXISTS (
        SELECT 1 FROM "EMPLOYEE" m
        WHERE m.employee_id = NEW.manager_employee_id
          AND m.employee_id <> NEW.employee_id
          AND m.status = 'ACTIVE' AND m.deleted_at IS NULL
          AND m.hire_date <= NEW.effective_from
          AND (m.retirement_date IS NULL OR (
              NEW.effective_to IS NOT NULL
              AND NEW.effective_to < m.retirement_date
          ))
    ))
)
BEGIN
    SELECT RAISE(ABORT, 'ASSIGNMENT_REFERENCE_INVALID');
END;

CREATE TRIGGER "trg_assignment_reference_bu"
BEFORE UPDATE OF employee_id, organization_id, position_id,
                 manager_employee_id, effective_from, effective_to, deleted_at
ON "EMPLOYEE_ASSIGNMENT"
WHEN NEW.deleted_at IS NULL AND (
    NOT EXISTS (
        SELECT 1 FROM "ORGANIZATION" o
        WHERE o.organization_id = NEW.organization_id
          AND o.active = 1 AND o.deleted_at IS NULL
          AND o.effective_from <= NEW.effective_from
          AND (o.effective_to IS NULL OR (
              NEW.effective_to IS NOT NULL
              AND o.effective_to >= NEW.effective_to
          ))
    )
    OR NOT EXISTS (
        SELECT 1 FROM "POSITION" p
        WHERE p.position_id = NEW.position_id
          AND p.active = 1 AND p.deleted_at IS NULL
          AND p.effective_from <= NEW.effective_from
          AND (p.effective_to IS NULL OR (
              NEW.effective_to IS NOT NULL
              AND p.effective_to >= NEW.effective_to
          ))
    )
    OR (NEW.manager_employee_id IS NOT NULL AND NOT EXISTS (
        SELECT 1 FROM "EMPLOYEE" m
        WHERE m.employee_id = NEW.manager_employee_id
          AND m.employee_id <> NEW.employee_id
          AND m.status = 'ACTIVE' AND m.deleted_at IS NULL
          AND m.hire_date <= NEW.effective_from
          AND (m.retirement_date IS NULL OR (
              NEW.effective_to IS NOT NULL
              AND NEW.effective_to < m.retirement_date
          ))
    ))
)
BEGIN
    SELECT RAISE(ABORT, 'ASSIGNMENT_REFERENCE_INVALID');
END;

CREATE TRIGGER "trg_employee_manager_reference_bu"
BEFORE UPDATE OF retirement_date, status, deleted_at ON "EMPLOYEE"
WHEN NEW.retirement_date IS NOT NULL AND EXISTS (
    SELECT 1 FROM "EMPLOYEE_ASSIGNMENT" a
    WHERE a.manager_employee_id = NEW.employee_id
      AND a.deleted_at IS NULL
      AND (a.effective_to IS NULL OR a.effective_to >= NEW.retirement_date)
)
BEGIN
    SELECT RAISE(ABORT, 'MANAGER_REFERENCE_EXISTS');
END;

CREATE TRIGGER "trg_organization_reference_bi"
BEFORE INSERT ON "ORGANIZATION"
BEGIN
    SELECT CASE WHEN NEW.parent_organization_id = NEW.organization_id
        THEN RAISE(ABORT, 'ORGANIZATION_REFERENCE_INVALID') END;
    SELECT CASE WHEN NEW.deleted_at IS NULL AND NEW.active = 1
        AND NEW.parent_organization_id IS NOT NULL AND NOT EXISTS (
            SELECT 1 FROM "ORGANIZATION" parent
            WHERE parent.organization_id = NEW.parent_organization_id
              AND parent.active = 1 AND parent.deleted_at IS NULL
              AND parent.effective_from <= NEW.effective_from
              AND (parent.effective_to IS NULL OR (
                  NEW.effective_to IS NOT NULL
                  AND parent.effective_to >= NEW.effective_to
              ))
        ) THEN RAISE(ABORT, 'ORGANIZATION_REFERENCE_INVALID') END;
END;

CREATE TRIGGER "trg_organization_reference_bu"
BEFORE UPDATE OF parent_organization_id, effective_from, effective_to,
                 active, deleted_at ON "ORGANIZATION"
BEGIN
    SELECT CASE WHEN NEW.parent_organization_id = NEW.organization_id
        THEN RAISE(ABORT, 'ORGANIZATION_REFERENCE_INVALID') END;
    SELECT CASE WHEN NEW.deleted_at IS NULL AND NEW.active = 1
        AND NEW.parent_organization_id IS NOT NULL AND NOT EXISTS (
            SELECT 1 FROM "ORGANIZATION" parent
            WHERE parent.organization_id = NEW.parent_organization_id
              AND parent.active = 1 AND parent.deleted_at IS NULL
              AND parent.effective_from <= NEW.effective_from
              AND (parent.effective_to IS NULL OR (
                  NEW.effective_to IS NOT NULL
                  AND parent.effective_to >= NEW.effective_to
              ))
        ) THEN RAISE(ABORT, 'ORGANIZATION_REFERENCE_INVALID') END;
    SELECT CASE WHEN NEW.parent_organization_id IS NOT NULL AND EXISTS (
        WITH RECURSIVE ancestors(organization_id) AS (
            SELECT NEW.parent_organization_id
            UNION
            SELECT o.parent_organization_id
            FROM "ORGANIZATION" o
            JOIN ancestors a ON o.organization_id = a.organization_id
            WHERE o.parent_organization_id IS NOT NULL
              AND o.deleted_at IS NULL
        )
        SELECT 1 FROM ancestors
        WHERE organization_id = NEW.organization_id
    ) THEN RAISE(ABORT, 'ORGANIZATION_REFERENCE_INVALID') END;
    SELECT CASE WHEN EXISTS (
        SELECT 1 FROM "ORGANIZATION" child
        WHERE child.parent_organization_id = NEW.organization_id
          AND child.active = 1 AND child.deleted_at IS NULL
          AND (
              NEW.active <> 1 OR NEW.deleted_at IS NOT NULL
              OR NEW.effective_from > child.effective_from
              OR (NEW.effective_to IS NOT NULL AND (
                  child.effective_to IS NULL
                  OR NEW.effective_to < child.effective_to
              ))
          )
    ) THEN RAISE(ABORT, 'ORGANIZATION_REFERENCE_INVALID') END;
    SELECT CASE WHEN EXISTS (
        SELECT 1 FROM "EMPLOYEE_ASSIGNMENT" a
        WHERE a.organization_id = NEW.organization_id
          AND a.deleted_at IS NULL
          AND (
              NEW.active <> 1 OR NEW.deleted_at IS NOT NULL
              OR NEW.effective_from > a.effective_from
              OR (NEW.effective_to IS NOT NULL AND (
                  a.effective_to IS NULL OR NEW.effective_to < a.effective_to
              ))
          )
    ) THEN RAISE(ABORT, 'ORGANIZATION_REFERENCE_INVALID') END;
END;

CREATE TRIGGER "trg_position_reference_bu"
BEFORE UPDATE OF effective_from, effective_to, active, deleted_at ON "POSITION"
WHEN EXISTS (
    SELECT 1 FROM "EMPLOYEE_ASSIGNMENT" a
    WHERE a.position_id = NEW.position_id
      AND a.deleted_at IS NULL
      AND (
          NEW.active <> 1 OR NEW.deleted_at IS NOT NULL
          OR NEW.effective_from > a.effective_from
          OR (NEW.effective_to IS NOT NULL AND (
              a.effective_to IS NULL OR NEW.effective_to < a.effective_to
          ))
      )
)
BEGIN
    SELECT RAISE(ABORT, 'POSITION_REFERENCE_INVALID');
END;

CREATE TRIGGER "trg_user_role_reference_bi"
BEFORE INSERT ON "USER_ROLE"
WHEN NEW.deleted_at IS NULL AND (
    NOT EXISTS (
        SELECT 1 FROM "USER_ACCOUNT" ua
        WHERE ua.user_id = NEW.user_id
          AND ua.active = 1 AND ua.deleted_at IS NULL
    )
    OR NOT EXISTS (
        SELECT 1 FROM "ROLE" r
        WHERE r.role_id = NEW.role_id
          AND r.active = 1 AND r.deleted_at IS NULL
    )
)
BEGIN
    SELECT RAISE(ABORT, 'USER_ROLE_REFERENCE_INVALID');
END;

CREATE TRIGGER "trg_user_role_reference_bu"
BEFORE UPDATE OF user_id, role_id, effective_from, effective_to, deleted_at
ON "USER_ROLE"
WHEN NEW.deleted_at IS NULL AND (
    NOT EXISTS (
        SELECT 1 FROM "USER_ACCOUNT" ua
        WHERE ua.user_id = NEW.user_id
          AND ua.active = 1 AND ua.deleted_at IS NULL
    )
    OR NOT EXISTS (
        SELECT 1 FROM "ROLE" r
        WHERE r.role_id = NEW.role_id
          AND r.active = 1 AND r.deleted_at IS NULL
    )
)
BEGIN
    SELECT RAISE(ABORT, 'USER_ROLE_REFERENCE_INVALID');
END;
```

## 4.13 トランザクション境界

| TX-ID | 業務処理・公開IF | Prepared Statement順 | D1実行方式 | 成功条件 | 全体ロールバック条件 | 公開例外 |
|---|---|---|---|---|---|---|
| TX-001 | 社員登録 / M-002→M-006/IF-05 | SQL-005→SQL-006→SQL-007 | `batch()` | 重複・組織/役職有効性を満たし全Statement成功 | いずれかのStatement失敗・一意/参照/CHECK違反 | DUPLICATED、MASTER_NOT_ACTIVE、DATA_ACCESS_ERROR |
| TX-002 | 社員基本情報更新 / M-002→M-006/IF-08 | SQL-008-A(guard INSERT)→SQL-008-B(EMPLOYEE更新)→SQL-007→SQL-008-C(guard削除) | `batch()` | 所有者版数一致・全Statement成功 | guard不成立(所有者更新0件)・制約違反 | CONFLICT、EMAIL_DUPLICATED、DATA_ACCESS_ERROR |
| TX-003 | 社員異動 / M-002→M-006/IF-34 | SQL-034-A(guard)→SQL-034-B(版数更新)→SQL-010(直前終了・将来取消)→SQL-006(新所属)→SQL-007→SQL-034-C(guard削除) | `batch()` | 所有者版数一致・期間/上長条件成立・全Statement成功 | guard不成立・期間/階層/一意違反 | CONFLICT、DATA_ACCESS_ERROR |
| TX-004 | 未来日退職受付 / M-002→M-006/IF-11 | SQL-011-A(guard)→SQL-011-B(退職日・区分・版数更新)→SQL-007→SQL-011-C(guard削除) | `batch()` | 所有者版数一致・上長逆参照なし・状態条件成立 | guard不成立・上長逆参照存在・状態条件不成立 | CONFLICT、INVALID_RETIREMENT_DATE、DATA_ACCESS_ERROR |
| TX-005 | 即時・到来退職反映 / M-002→M-006/IF-13 | SQL-013-A(guard)→SQL-013-B(退職確定)→SQL-010(所属終了・将来取消)→SQL-007→SQL-013-C(guard削除) | `batch()`（社員1件ごと） | 所有者版数一致・上長逆参照なし・全Statement成功 | guard不成立・上長逆参照存在・状態条件不成立（当該社員のバッチ） | CONFLICT、INVALID_RETIREMENT_DATE、DATA_ACCESS_ERROR |
| TX-006 | 組織登録・更新 / M-002→M-006/IF-19(登録)・IF-20(更新) | 登録=SQL-019 / 更新=SQL-020-A(guard)→SQL-020-B(条件付き更新)→SQL-020-C(guard削除) | 登録=`run()` / 更新=`batch()` | 更新は所有者版数一致・親子期間/循環/所属参照条件成立 | guard不成立・循環/期間/参照違反 | CONFLICT、CODE_DUPLICATED、DATA_ACCESS_ERROR |
| TX-007 | 役職登録・更新 / M-002→M-006/IF-24(登録)・IF-25(更新) | 登録=SQL-024 / 更新=SQL-025-A(guard)→SQL-025-B(条件付き更新)→SQL-025-C(guard削除) | 登録=`run()` / 更新=`batch()` | 更新は所有者版数一致・所属参照条件成立 | guard不成立・期間/参照違反 | CONFLICT、CODE_DUPLICATED、DATA_ACCESS_ERROR |
| TX-008 | ロール割当更新 / M-002→M-006/IF-35 | SQL-035-A(guard)→SQL-035-B(USER_ACCOUNT版数更新)→SQL-029(現行終了)→SQL-030(新規登録)→SQL-007→SQL-035-C(guard削除) | `batch()` | 所有者版数一致・期間条件成立・全Statement成功 | guard不成立・期間重複(trigger)・状態条件不成立 | CONFLICT、ROLE_ASSIGNMENT_PERIOD_CONFLICT、DATA_ACCESS_ERROR |

- M-006はバッチ内statementを表の記載順にprepared statementとして配列化する。D1は同一DB内で順次実行し、全statement成功時だけ確定する。M-006からSQLの`BEGIN`、`COMMIT`、`ROLLBACK`は発行しない。
- AUDIT_LOGは業務更新の成功後に独立した単一文`run()`で追記する。
- AUDIT_LOG記録に失敗した場合、業務更新は巻き戻さず、M-007が監査記録失敗を返して運用アラート対象とする。
- 即時再試行は§6.3.5に列挙した5エラーだけを対象に、指数バックオフ＋full jitterで初回後最大2回とする。実行時・デプロイ時の追加は許可しない。書込結果不明は状態・versionをM-006経由で再参照してcommit確認後、未反映だけ新しい`guard_id`で再試行する。overload、timeout、CPU・memory超過は即時再試行せず、M-002がオンラインは503系の一時障害、JOBだけQueue再配信へ変換する。制約・トリガー・版数・集合競合は再試行しない。
- JOB-001はSQL-012を`limit + 1`で最大41件取得し、先頭40名だけを1 Worker invocationの1チャンクとして処理する。1名当たり最大21 statement、ページ取得1 statementの最悪値は`21 × 40 + 1 = 841`で、内部上限900およびD1 Paid上限1,000を下回る。41件目は処理せず`hasMore`と`nextCursor`の判定にだけ使う。
- M-006は`first()`、`all()`、`run()`、`batch()`でD1へ送信したprepared statement数を実測し、`statementCount`（互換名`queryCount`）としてM-002へ返す。900到達前に新たな社員処理を開始しない。

### 4.13.1 所有者version・更新集合ガード

事前SELECTと更新バッチの間に別要求が確定しても、古い判定結果で更新しないよう、次の技術テーブルを作成する。成功時は同じバッチの末尾で削除され、途中失敗時はバッチとともにINSERT自体が取り消されるため残留しない。

```sql
CREATE TABLE "TX_MUTATION_GUARD" (
    guard_id TEXT PRIMARY KEY,
    owner_type TEXT NOT NULL
        CHECK (owner_type IN ('NONE', 'EMPLOYEE', 'ORGANIZATION', 'POSITION', 'USER_ACCOUNT')),
    owner_id TEXT,
    expected_version INTEGER,
    operation_type TEXT NOT NULL
        CHECK (operation_type IN ('NONE', 'ASSIGNMENT_TRANSFER', 'ASSIGNMENT_RETIREMENT', 'USER_ROLE_REPLACE')),
    pivot_date TEXT,
    expected_target_ids_json TEXT NOT NULL
        CHECK (json_valid(expected_target_ids_json) AND json_type(expected_target_ids_json) = 'array'),
    retained_ids_json TEXT NOT NULL DEFAULT '[]'
        CHECK (json_valid(retained_ids_json) AND json_type(retained_ids_json) = 'array'),
    created_at TEXT NOT NULL,
    CHECK (
        (owner_type = 'NONE' AND owner_id IS NULL AND expected_version IS NULL)
        OR
        (owner_type <> 'NONE' AND owner_id IS NOT NULL AND expected_version >= 1)
    ),
    CHECK (
        (operation_type = 'NONE' AND pivot_date IS NULL)
        OR
        (operation_type <> 'NONE' AND pivot_date IS NOT NULL)
    )
) STRICT;

CREATE TRIGGER "trg_mutation_guard_owner_bi"
BEFORE INSERT ON "TX_MUTATION_GUARD"
BEGIN
    SELECT CASE
        WHEN NEW.owner_type = 'EMPLOYEE' AND NOT EXISTS (
            SELECT 1 FROM "EMPLOYEE"
            WHERE employee_id = NEW.owner_id
              AND version = NEW.expected_version
              AND deleted_at IS NULL
        ) THEN RAISE(ABORT, 'OWNER_VERSION_CONFLICT')
        WHEN NEW.owner_type = 'ORGANIZATION' AND NOT EXISTS (
            SELECT 1 FROM "ORGANIZATION"
            WHERE organization_id = NEW.owner_id
              AND version = NEW.expected_version
              AND deleted_at IS NULL
        ) THEN RAISE(ABORT, 'OWNER_VERSION_CONFLICT')
        WHEN NEW.owner_type = 'POSITION' AND NOT EXISTS (
            SELECT 1 FROM "POSITION"
            WHERE position_id = NEW.owner_id
              AND version = NEW.expected_version
              AND deleted_at IS NULL
        ) THEN RAISE(ABORT, 'OWNER_VERSION_CONFLICT')
        WHEN NEW.owner_type = 'USER_ACCOUNT' AND NOT EXISTS (
            SELECT 1 FROM "USER_ACCOUNT"
            WHERE user_id = NEW.owner_id
              AND version = NEW.expected_version
              AND deleted_at IS NULL
        ) THEN RAISE(ABORT, 'OWNER_VERSION_CONFLICT')
    END;
    SELECT CASE
        WHEN EXISTS (
            SELECT 1 FROM json_each(NEW.expected_target_ids_json) WHERE type <> 'text'
        ) OR (
            SELECT count(*) FROM json_each(NEW.expected_target_ids_json)
        ) <> (
            SELECT count(DISTINCT value) FROM json_each(NEW.expected_target_ids_json)
        ) THEN RAISE(ABORT, 'EXPECTED_SET_INVALID')
    END;
    SELECT CASE
        WHEN NEW.operation_type IN ('ASSIGNMENT_TRANSFER', 'ASSIGNMENT_RETIREMENT')
         AND NEW.owner_type <> 'EMPLOYEE'
          THEN RAISE(ABORT, 'EXPECTED_SET_INVALID')
        WHEN NEW.operation_type = 'USER_ROLE_REPLACE'
         AND NEW.owner_type <> 'USER_ACCOUNT'
          THEN RAISE(ABORT, 'EXPECTED_SET_INVALID')
        WHEN NEW.operation_type = 'NONE'
         AND json_array_length(NEW.expected_target_ids_json) <> 0
          THEN RAISE(ABORT, 'EXPECTED_SET_INVALID')
    END;
END;
```

更新集合検証トリガーは、次の集合を`expected_target_ids_json`と双方向に比較し、片側に1件でも差があれば`UPDATE_SET_CONFLICT`で中止する。`USER_ROLE_REPLACE`では`retained_ids_json`も重複なしTEXT配列として同じ要領で検証する。

| operation_type | DB内で再計算する更新対象集合 |
|---|---|
| `ASSIGNMENT_TRANSFER` | `owner_id`の未削除所属のうち、`pivot_date`前日に有効な行、または`effective_from >= pivot_date`の行 |
| `ASSIGNMENT_RETIREMENT` | `owner_id`の未削除所属のうち、`pivot_date`当日に有効な行、または`effective_from > pivot_date`の行 |
| `USER_ROLE_REPLACE` | `owner_id`の未削除ロールのうち、`effective_to IS NULL OR effective_to >= pivot_date`で、`retained_ids_json`に含まれない行 |

次のD1/SQLiteトリガーで3種類の集合を実際に双方向突合する。片方向だけの件数比較ではなく、DB集合にだけ存在するIDと期待集合にだけ存在するIDの両方を検出する。

```sql
CREATE TRIGGER "trg_mutation_guard_set_bi"
BEFORE INSERT ON "TX_MUTATION_GUARD"
WHEN NEW.operation_type <> 'NONE'
BEGIN
    SELECT CASE
        WHEN EXISTS (
            SELECT 1 FROM json_each(NEW.retained_ids_json) WHERE type <> 'text'
        ) OR (
            SELECT count(*) FROM json_each(NEW.retained_ids_json)
        ) <> (
            SELECT count(DISTINCT value) FROM json_each(NEW.retained_ids_json)
        ) THEN RAISE(ABORT, 'EXPECTED_SET_INVALID')
    END;

    SELECT CASE
        WHEN NEW.operation_type = 'ASSIGNMENT_TRANSFER' AND (
            EXISTS (
                SELECT 1
                FROM "EMPLOYEE_ASSIGNMENT" a
                WHERE a.employee_id = NEW.owner_id
                  AND a.deleted_at IS NULL
                  AND (
                      (
                          a.effective_from <= date(NEW.pivot_date, '-1 day')
                          AND (a.effective_to IS NULL OR a.effective_to >= date(NEW.pivot_date, '-1 day'))
                      )
                      OR a.effective_from >= NEW.pivot_date
                  )
                  AND NOT EXISTS (
                      SELECT 1 FROM json_each(NEW.expected_target_ids_json) j
                      WHERE CAST(j.value AS TEXT) = a.assignment_id
                  )
            )
            OR EXISTS (
                SELECT 1
                FROM json_each(NEW.expected_target_ids_json) j
                WHERE NOT EXISTS (
                    SELECT 1
                    FROM "EMPLOYEE_ASSIGNMENT" a
                    WHERE a.assignment_id = CAST(j.value AS TEXT)
                      AND a.employee_id = NEW.owner_id
                      AND a.deleted_at IS NULL
                      AND (
                          (
                              a.effective_from <= date(NEW.pivot_date, '-1 day')
                              AND (a.effective_to IS NULL OR a.effective_to >= date(NEW.pivot_date, '-1 day'))
                          )
                          OR a.effective_from >= NEW.pivot_date
                      )
                )
            )
        ) THEN RAISE(ABORT, 'UPDATE_SET_CONFLICT')

        WHEN NEW.operation_type = 'ASSIGNMENT_RETIREMENT' AND (
            EXISTS (
                SELECT 1
                FROM "EMPLOYEE_ASSIGNMENT" a
                WHERE a.employee_id = NEW.owner_id
                  AND a.deleted_at IS NULL
                  AND (
                      (
                          a.effective_from <= NEW.pivot_date
                          AND (a.effective_to IS NULL OR a.effective_to >= NEW.pivot_date)
                      )
                      OR a.effective_from > NEW.pivot_date
                  )
                  AND NOT EXISTS (
                      SELECT 1 FROM json_each(NEW.expected_target_ids_json) j
                      WHERE CAST(j.value AS TEXT) = a.assignment_id
                  )
            )
            OR EXISTS (
                SELECT 1
                FROM json_each(NEW.expected_target_ids_json) j
                WHERE NOT EXISTS (
                    SELECT 1
                    FROM "EMPLOYEE_ASSIGNMENT" a
                    WHERE a.assignment_id = CAST(j.value AS TEXT)
                      AND a.employee_id = NEW.owner_id
                      AND a.deleted_at IS NULL
                      AND (
                          (
                              a.effective_from <= NEW.pivot_date
                              AND (a.effective_to IS NULL OR a.effective_to >= NEW.pivot_date)
                          )
                          OR a.effective_from > NEW.pivot_date
                      )
                )
            )
        ) THEN RAISE(ABORT, 'UPDATE_SET_CONFLICT')

        WHEN NEW.operation_type = 'USER_ROLE_REPLACE' AND (
            EXISTS (
                SELECT 1
                FROM "USER_ROLE" ur
                WHERE ur.user_id = NEW.owner_id
                  AND ur.deleted_at IS NULL
                  AND (ur.effective_to IS NULL OR ur.effective_to >= NEW.pivot_date)
                  AND NOT EXISTS (
                      SELECT 1 FROM json_each(NEW.retained_ids_json) r
                      WHERE CAST(r.value AS TEXT) = ur.user_role_id
                  )
                  AND NOT EXISTS (
                      SELECT 1 FROM json_each(NEW.expected_target_ids_json) j
                      WHERE CAST(j.value AS TEXT) = ur.user_role_id
                  )
            )
            OR EXISTS (
                SELECT 1
                FROM json_each(NEW.expected_target_ids_json) j
                WHERE NOT EXISTS (
                    SELECT 1
                    FROM "USER_ROLE" ur
                    WHERE ur.user_role_id = CAST(j.value AS TEXT)
                      AND ur.user_id = NEW.owner_id
                      AND ur.deleted_at IS NULL
                      AND (ur.effective_to IS NULL OR ur.effective_to >= NEW.pivot_date)
                      AND NOT EXISTS (
                          SELECT 1 FROM json_each(NEW.retained_ids_json) r
                          WHERE CAST(r.value AS TEXT) = ur.user_role_id
                      )
                )
            )
        ) THEN RAISE(ABORT, 'UPDATE_SET_CONFLICT')
    END;
END;
```

M-006は集合ガード成功後のUPDATE対象を広い日付条件で再指定せず、`expected_target_ids_json`を`json_each()`で展開したIDだけに限定する。所有者の版数更新は`WHERE 主キー = owner_id AND version = expected_version`で1件だけ更新する。これにより、ガード検証後からバッチ末尾までの逐次実行中に対象集合がずれない。

バッチ成功後、M-006は所有者更新statementの件数が1件であること、guard削除も1件であることを確認する。差分更新SQL-010/SQL-029は設計上0件以上となり得るため、`RETURNING` ID集合と`expected_target_ids_json`の完全一致は求めない。基準日に既に同日終了済みの自所属が更新対象から外れる正常差分（§8.1.2・SQL-010注意）は整合性事故として扱わず、`expected_target_ids_json`に含まれ更新されなかったIDは既終了として許容する。SQL-006/SQL-007/SQL-030など呼出計画上の期待件数を持つstatementは期待件数と`results`件数を照合し、突合できない件数差だけを通常到達不能な整合性事故として記録して後続処理を停止する。業務上の競合は必ずバッチ内ガードで検出して全体を取り消す。

## 4.14 共通カラム

### 更新可能テーブル共通

EMPLOYEE、EMPLOYEE_ASSIGNMENT、ORGANIZATION、POSITION、USER_ACCOUNT、ROLE、USER_ROLEへ付与する。

| カラム | 論理名 | SQLite型 | NULL | DEFAULT | 制約・保存形式 |
|---|---|---|---|---|---|
| created_at | 登録日時 | TEXT | 不可 | なし | UTCのISO 8601日時。登録時にM-006がバインド |
| created_by | 登録者ID | TEXT | 可 | なし | UUID正準表現。JOB・システム初期データ・AUTH認証前失敗ではNULL可 |
| updated_at | 更新日時 | TEXT | 不可 | なし | UTCのISO 8601日時。更新時にM-006がバインド |
| updated_by | 更新者ID | TEXT | 可 | なし | UUID正準表現。JOB実行時はNULL可 |
| version | 更新バージョン | INTEGER | 不可 | 1 | `CHECK (version >= 1)`。初期値1、更新成功時に1加算 |
| deleted_at | 論理削除日時 | TEXT | 可 | NULL | UTCのISO 8601日時。NULL=有効。物理削除は禁止 |

### 追記専用テーブル共通

EMPLOYEE_CHANGE_HISTORY、AUDIT_LOGへ付与する。

| カラム | 論理名 | SQLite型 | NULL | DEFAULT | 制約・保存形式 |
|---|---|---|---|---|---|
| created_at | 登録日時 | TEXT | 不可 | なし | UTCのISO 8601日時。追記時にM-006がバインド |
| created_by | 登録者ID | TEXT | 可 | なし | UUID正準表現。JOB実行時またはAUTH認証前失敗ではNULL可 |

## 4.15 命名規則

| 対象 | 規則 |
|---|---|
| テーブル | 英大文字スネークケース、単数形。履歴は`_HISTORY`、ログは`_LOG` |
| カラム | 英小文字スネークケース |
| 主キー | `{entity}_id` |
| 外部キー | 参照先の主キー名。用途が異なる自己参照は用途を接頭する |
| 制約 | `pk_` / `fk_` / `uk_` / `ck_` + 小文字テーブル名 + 用途 |
| トリガー | `trg_` + 小文字テーブル名 + 用途 + `_bi`（INSERT前）/ `_bu`（UPDATE前）/ `_ai`（INSERT後）/ `_au`（UPDATE後） |
| インデックス | `idx_` + 小文字テーブル名 + 主要列または用途 |
| バインド変数 | 08章の実SQLはD1 ordered placeholder `?1`、`?2`…だけを使用し、Prepared Statementごとの固定バインド順でコロンなし論理入力名との対応を定義する |
| Migration ID | `MIG-`＋3桁連番。§4.16のD1 migration単位に付番する |
| 原子実行境界 | `TX-`＋3桁連番。§4.13の1回のD1バッチで確定する原子実行単位に付番する |

- SQLはテーブル名を二重引用符で囲み、本書の英大文字物理名と厳密に一致させる。
- 同じ論理パラメーターを複数箇所で使う場合も位置番号と値の対応表をPrepared Statementごとに固定し、複数構成文では各文を`?1`から欠番なく採番し直す。文字列置換でSQLを組み立てず、値はすべて`D1PreparedStatement.bind()`へ渡す。
- 文字列連結は`||`、NULL置換は`coalesce()`、日付加減算は`date(date_text, '+1 day')`または`date(date_text, '-1 day')`を使用する。
- 配列バインドは使用せず、JSON配列`TEXT`をバインドして`json_each(json_array)`で展開する。
- 現在日時はWorkers側で1要求につき一度生成し、同一バッチの全statementへ同じISO 8601文字列をバインドする。
- 列挙コードは大文字スネークケースとし、意味は機能要件・共通定義を正本とする。

## 4.16 D1 migration・検証引継ぎ

DDL・固定データ・トリガーはD1 migrationとして版管理し、Workers起動時やAPI・JOBから実行しない。migrationの適用は承認済みデプロイ工程だけが行い、アプリケーション実行時のDBアクセスは引き続きM-006だけに限定する。

| Migration ID | 適用順 | 変更内容 | 前提 | 適用後検証・期待値 | 復旧・前進修正方針 |
|---|---:|---|---|---|---|
| MIG-001 | 1 | EMPLOYEE〜AUDIT_LOGおよびTX_MUTATION_GUARDを外部キー・CHECK・UNIQUE付き`STRICT` tableとして作成する | 空DBへ最初に適用（先行migrationなし） | `pragma_table_list`で対象10表の`strict = 1`、`PRAGMA foreign_key_check`が0行 | 未公開環境はmigration取消し。本番適用後は破壊的rollbackを行わず前進修正 |
| MIG-002 | 2 | IDX-001〜IDX-022、TRG-001〜TRG-005の物理トリガー、更新集合ガードトリガーを作成し、ROLE固定4件を冪等投入する | MIG-001適用済み | index/triggerの件数と名称が本章に一致、ROLEが4件、全negative testが固定エラーで拒否 | 欠落定義を追加migrationで補完し、適用済みDDLを手作業変更しない |
| MIG-003 | 任意 | 性能移行基準を満たした場合だけFTS-001（EMPLOYEE_SEARCH_FTS）と同期トリガーを作成して再構築する | MIG-001・MIG-002適用済みかつ性能移行基準を充足 | 有効EMPLOYEE件数とFTS件数が一致し、代表検索結果がSQL-001/032の初期正本と一致 | 読取経路を初期`instr()`へ戻した後、追加migrationでFTS定義を修正 |

- ローカルD1互換SQLiteとpreview D1の両方で、全migrationを空DBへ順適用し、通常・技術テーブルが`STRICT`であることを検証する。
- 型不一致、日付形式、列挙外コード、期間重複、組織循環、参照中マスター無効化、退職予定済み上長の新規指定をnegative testに含める。
- destructiveなSQLite schema変更は、新`STRICT` table作成、データ変換、件数・制約検証、切替、旧table廃止を別migrationとして順序化する。
- 他DBMSのdumpやDDLをそのまま投入せず、SQLite物理型・CHECK・triggerへ変換してから検証する。
