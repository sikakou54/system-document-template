<!-- コピーして 02_機能設計/02_画面設計/SCR-XXX_画面名.md として使用。index.md への行追加を先に行うこと -->
<!-- 表示文言は本文書末尾の「メッセージ一覧」節に MSG-XXX としてインライン定義する。本文各所では MSG-XXX で参照し、文言を重複記載しない -->
<!-- 各セクション直上のコメントに「定義内容(そのセクションの意味)」「定義する条件」「項目説明(各列・各項目の意味)」「定義ルール」をセットで記載する。編集時はコメントを読んでから該当セクションを埋める -->

<!--
【1. 基本情報】
定義内容: この画面の識別情報と属性(ID・名称・トレース元・URL・利用可能ロール・状態など)を一覧で示す。
定義する条件: 全画面で必須。
項目説明:
- 画面ID: この画面の識別子(SCR-XXX 連番)。
- 画面名: 画面の日本語名称。
- 概要: 画面の目的(1〜3行)。
- トレース元: この画面の実現元 UC-ID。
- URL / ルート: 画面のURLパスまたはルート。
- 利用可能ロール: この画面を利用できるロール。
定義ルール:
- 画面ID は SCR-XXX の連番。URL / ルートは実装と一致させる。
- トレース元に対応する UC-ID を記載する。
- 利用可能ロールはロール名を明記する(例: 一般, 管理者)。
-->
## 1. 基本情報

| 項目 | 内容 |
|---|---|
| 画面ID | SCR-XXX |
| 画面名 |  |
| 概要 | (1〜3行) |
| トレース元 | UC-XXX |
| URL / ルート |  |
| 利用可能ロール |  |

<!--
【2. 画面レイアウト】
定義内容: この画面の見た目・レイアウトを、モック画面の画像で示す。項目の配置・並び・グループ構成を視覚的に把握できるようにする。
定義する条件: 全画面で必須。画面の配置・構成が分かるモック画像を1枚以上貼る。
項目説明:
- モック画像: 画面のモックを HTML で作成し PNG 化した画像。ファイルは mockups/ 配下に置く。
- キャプション: 画像記法の代替テキスト。画面名や状態(通常時・0件時・エラー時など)を記す。
定義ルール:
- モックは HTML で作成し、PNG に書き出して mockups/SCR-XXX.png として保存する。Markdown の画像記法 ![キャプション](mockups/SCR-XXX.png) で貼る。
- レイアウト(配置・構成)を示すことを目的とし、画像内の文言は代表例でよい(確定した文言は本文書の「メッセージ一覧」節が正本)。
- 状態違い(通常・0件・エラーなど)を複数見せる場合は画像を複数貼り、各画像にキャプションを付ける。
- 各項目の詳細(種別・必須・初期値など)は 画面項目 セクションで定義する(画像は配置把握が目的)。
-->
## 2. 画面レイアウト

![画面名 画面レイアウト](mockups/SCR-XXX.png)

<!--
【3. 初期表示】
定義内容: 画面表示時に呼び出すAPIと、初期状態(デフォルト値・ソート順・0件時の表示)を示す。
定義する条件: 全画面で必須。表示時にAPIを呼び出す場合は呼び出す API-ID を記載し、無ければ「なし」とする。
項目説明:
- 表示時に呼び出すAPI: 画面表示時に呼び出す API-ID。
- デフォルト値: 初期表示時に各項目へ設定する値。
- ソート順: 一覧表示の並び順。
- 0件時の表示: データが0件のときの表示内容(MSG-XXX 参照可)。
定義ルール:
- 呼び出すAPIは API-ID で参照する(呼び出しが無ければ「なし」)。
- 0件時の表示に文言を出す場合は本文書「メッセージ一覧」節の MSG-XXX を参照する。
-->
## 3. 初期表示

| 項目 | 内容 |
|---|---|
| 表示時に呼び出すAPI | API-XXX |
| デフォルト値 |  |
| ソート順 |  |
| 0件時の表示 |  |

<!--
【4. 画面項目】
定義内容: この画面に配置する各項目(入力・表示・ボタン)の一覧と属性を示す。
定義する条件: 画面に項目がある場合に定義する。項目ごとに1行を記載する。
項目説明:
- 項目ID: 画面内で一意な項目ID(ITM-XX 連番)。
- 項目名: 項目の日本語表示名。
- 種別: 項目の種類(text / select / date / button など)。
- 表示/入力: 表示専用か入力可能か(表示 / 入力)。
- 必須: 入力必須かどうか(Yes / No)。
- 初期値: 初期表示時の値。
- 備考: 補足(選択肢の TBL-XXX/ENM-x 参照など)。
定義ルール:
- 項目ID は ITM-XX の連番で画面内一意とする。
- 種別は text / select / date / button などから選ぶ。
- 選択肢が区分値の場合は 定義先テーブル文書の列挙値(TBL-XXX/ENM-x)を参照する。
-->
## 4. 画面項目

| 項目ID | 項目名 | 種別 | 表示/入力 | 必須 | 初期値 | 備考 |
|---|---|---|---|---|---|---|
| ITM-01 |  | text / select / date / button | 表示 / 入力 | Yes / No |  |  |

<!--
【5. 画面イベント】
定義内容: この画面で発生する操作イベント(ボタン押下など)と、発火条件・呼び出しAPI・成否時の挙動を示す。
定義する条件: 画面にユーザー操作によるイベントがある場合に定義する。イベントごとに1行を記載する。
項目説明:
- イベントID: 画面内で一意なローカルID(EVT-01 連番。他文書からは SCR-XXX/EVT-01 で参照)。
- イベント名: イベントの日本語名称。
- 発火条件: イベントが発生する契機(どの項目のどの操作か)。
- 呼び出しAPI: イベントで呼び出す API-ID。
- 成功時: 成功時の挙動(画面遷移・表示更新など)。
- 失敗時: 失敗時の挙動(MSG-XXX 表示など)。
定義ルール:
- イベントID は EVT-01 の連番で画面内一意とする。
- 呼び出しAPI は API-ID で参照する(呼び出しが無ければ「なし」)。
- 表示する文言は本文書「メッセージ一覧」節の MSG-XXX を参照し、文言を再記載しない。
-->
## 5. 画面イベント

| イベントID | イベント名 | 発火条件 | 呼び出しAPI | 成功時 | 失敗時 |
|---|---|---|---|---|---|
| EVT-01 |  |  | API-XXX |  | MSG-XXX 表示 |

<!--
【6. 入力チェック】
定義内容: 画面上のクライアント側入力チェックの対象項目・チェック内容・表示メッセージを示す。
定義する条件: 画面でクライアント側チェックを行う項目がある場合に定義する。サーバ側バリデーションは API 文書に記載する。
項目説明:
- 対象項目: チェック対象の項目(画面項目の項目名)。
- チェック内容: 正常とみなす条件・チェックの内容。
- 表示メッセージ: 違反時に表示する MSG-XXX。
定義ルール:
- クライアント側チェックのみを記載し、サーバ側バリデーションは API 文書に定義する。
- 表示メッセージは本文書「メッセージ一覧」節の MSG-XXX を参照し、文言を再記載しない。
-->
## 6. 入力チェック

<!-- クライアント側チェックのみ。サーバ側バリデーションは API 文書に記載 -->

| 対象項目 | チェック内容 | 表示メッセージ |
|---|---|---|
|  |  | MSG-XXX |

<!--
【7. 表示制御】
定義内容: 状態や条件に応じた項目の表示・非表示・活性・非活性の制御ルールを示す。
定義する条件: 条件によって項目の表示状態を切り替える場合に定義する。
項目説明:
- 条件: 制御を行う条件(ロール・状態・入力値など)。
- 対象: 制御対象の項目(画面項目の項目名)。
- 制御内容: 適用する制御(表示 / 非表示 / 活性 / 非活性)。
定義ルール:
- 制御内容は 表示 / 非表示 / 活性 / 非活性 のいずれかで記載する。
- 条件はロール・状態・入力値など判定可能な形で記載する。
-->
## 7. 表示制御

| 条件 | 対象 | 制御内容 |
|---|---|---|
|  |  | 表示 / 非表示 / 活性 / 非活性 |

<!--
【8. 画面遷移】
定義内容: この画面から他画面への遷移先と、その遷移を起こすトリガを示す。
定義する条件: 他画面への遷移がある場合に定義する。遷移先ごとに1行を記載する。
項目説明:
- 遷移先: 遷移先の画面ID(SCR-XXX)。
- トリガ: 遷移が起きる契機(イベント・操作)。
定義ルール:
- 遷移先は SCR-XXX で参照する。
- トリガは画面イベント(EVT-01)や操作で記載する。
-->
## 8. 画面遷移

| 遷移先 | トリガ |
|---|---|
| SCR-XXX |  |

<!--
【9. メッセージ一覧】
定義内容: この画面で表示するメッセージ(完了・確認・警告・エラー・情報)を MSG-XXX としてインライン定義する。本文各所(§3 初期表示・§5 画面イベント・§6 入力チェック・§7 表示制御)からは MSG-XXX で参照し、文言を重複記載しない。
定義する条件: 画面に表示するメッセージがある場合に定義する。無ければ行に「なし」を記載する。
項目説明:
- MSG ID: メッセージの識別子(MSG-XXX。ERR/MSG のインライン定義を含む全文書横断で最大値+1 に採番)。
- 種別: メッセージの種類(完了 / 確認 / 警告 / エラー / 情報)。
- 文言: 実際に画面表示する文言。この節を正本とし、本文では MSG-XXX で参照する。
- 対応ERR: エラーメッセージの場合、対応するエラーコード(ERR-XXX。共通エラーは API-COM_共通設計.md、固有エラーは該当API文書のインライン定義)。エラー起因でなければ「-」。
定義ルール:
- MSG ID の採番は全文書横断で最大値+1、欠番の再利用は禁止。
- 文言はこの節にのみ記載し、本文の他セクションでは MSG-XXX で参照する(文言の重複記載は禁止)。
- 対応ERR には、その MSG が表示される契機となる ERR-XXX を記載する(エラー起因でなければ「-」)。
-->
## 9. メッセージ一覧

| MSG ID | 種別 | 文言 | 対応ERR |
|---|---|---|---|
| MSG-XXX | 完了 / 確認 / 警告 / エラー / 情報 |  | ERR-XXX / - |
