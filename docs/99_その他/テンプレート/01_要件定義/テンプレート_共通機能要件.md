<!-- コピーして 01_要件定義/02_機能要件/CFR-XXX_要件名.md として使用。CFR-000_システム共通要件.md の共通機能要件一覧への行追加を先に行うこと -->
<!-- 共通機能要件(CFR)は、複数機能に横断して適用される機能要件。特定機能に固有の要件は FR-XXX に記載する -->
<!-- 各セクション直上のコメントに「定義内容(そのセクションの意味)」「定義する条件」「項目説明(各列・各項目の意味)」「定義ルール」をセットで記載する。編集時はコメントを読んでから該当セクションを埋める -->
<!-- 【フォーマット厳守】各表の列見出し(ヘッダ行)およびセクション構成(##/###)はテンプレート定義のまま変更しない。列の追加・削除・改名、見出しへの補足付記、テンプレートに無いセクション・表・小見出しの追加は禁止。定義済みの表には行の追加と各セルの内容記入のみを行う(docs/CLAUDE.md §8-1・§10-4)。補足はセル内の値、または各セクションの説明文(段落)に記載する -->

<!--
【1. 基本情報】
定義内容: この共通機能要件の識別情報(要件ID・要件名・概要・目的・適用範囲・対象ロール)を一覧で示す。
定義する条件: 全 CFR で必須。
項目説明:
- 要件ID: この共通機能要件の識別子(CFR-XXX 連番)。
- 要件名: 共通機能要件の日本語名称。
- 概要: この共通機能が全体に提供する内容(1〜3行)。
- 目的: この共通機能を設ける業務上のねらい・効果(1〜3行)。
- 適用範囲: この要件が適用される範囲(例: 全機能／利用者入力を受け付ける全機能／一覧表示を行う機能)。
- 対象ロール: この共通機能に関係する利用者区分(例: 全ユーザー／管理者)。
定義ルール:
- 要件ID は CFR-XXX の連番。採番は CFR-000 の共通機能要件一覧の最大値 +1(欠番の再利用は禁止)。
- CFR はトレース参照先を持たない(上流文書の ID を記載しない。用語・業務ルールの言及のみ可)。
- 概要・目的は1〜3行。画面項目・API仕様・DB項目・処理ロジックは書かない(how は機能設計で定義)。
-->
## 1. 基本情報

| 項目 | 内容 |
|---|---|
| 要件ID | CFR-XXX |
| 要件名 |  |
| 概要 | (1〜3行) |
| 目的 | (1〜3行) |
| 適用範囲 |  |
| 対象ロール |  |

<!--
【2. 共通機能要件】
定義内容: この共通機能が、適用範囲の各機能に対して課す共通の振る舞いを定義する。
定義する条件: 全 CFR で必須。
項目説明:
- 利用者の操作: 利用者がこの共通機能に関して行う操作・行為(該当しなければ「なし」)。
- システムの処理: システムが全体に対して共通して行う処理・振る舞い。
定義ルール:
- 業務レベルの what を書く。認証方式・エラー封筒・パラメータ名などの how は書かない(機能設計で定義)。
- 確認できた内容だけを記載し、未確認の内容は本文に入れない。
-->
## 2. 共通機能要件

適用範囲の各機能に共通して課す振る舞いを定義する。

| 項目 | 内容 |
|---|---|
| 利用者の操作 |  |
| システムの処理 |  |

<!--
【3. 適用条件】
定義内容: この共通機能が適用される対象と、適用されない例外を定義する。
定義する条件: 全 CFR で必須。
項目説明:
- 区分: 適用対象／適用外。
- 内容: どの機能・状況に適用される(されない)か。
定義ルール:
- 「適用外」には、この共通機能では扱わず各機能側で扱う範囲を書く(例: 業務判定は各 FR)。
- 画面・API・DB・処理ロジックの実装詳細は書かない。
-->
## 3. 適用条件

この共通機能が適用される対象と適用外を定義する。

| 区分 | 内容 |
|---|---|
| 適用対象 |  |
| 適用外 |  |

<!--
【4. 業務ルール】
定義内容: この共通機能の判断基準となる業務上のルールを定義する。
定義する条件: 判断基準となる業務ルールがある場合に定義する。無ければ「なし」を記載する。共通業務ルール(RULE-COM)に該当する場合は CFR-000 側で定義し、ここでは ID を言及するに留める。
項目説明:
- No: ルールの連番。
- ルール: 業務上守るべき判断基準・制約の内容。
定義ルール:
- 1行1ルールで、判断基準として使える具体的な条件を記載する。
- 画面・API・DB・処理ロジックの実装詳細は書かない。
-->
## 4. 業務ルール

この共通機能の判断基準となる業務上のルールを定義する。

| No | ルール |
|---|---|
| 1 |  |

<!--
【5. 受入条件】
定義内容: この要件を満たしていることを確認するための条件を定義する。
定義する条件: 全 CFR で必須。受入判定に用いる条件を記載する。
項目説明:
- No: 条件の連番。
- 条件: 要件の充足を確認できる、検証可能な条件。
定義ルール:
- 各条件は満たしたか否かを判定できる粒度で記載する。
- 業務観点の確認条件を書き、テストケースや画面・API仕様は書かない。
-->
## 5. 受入条件

この要件を満たしていることを確認する条件を定義する。

| No | 条件 |
|---|---|
| 1 |  |

<!--
【6. 前提条件】
定義内容: この要件が成り立つために、先に満たしておく必要がある条件を定義する。
定義する条件: 前提となる条件がある場合に定義する。無ければ No 列に「-」、条件列に「なし」を記載する。
項目説明:
- No: 条件の連番(無い場合は「-」)。
- 条件: この要件の成立前に満たしておくべき条件。
定義ルール:
- 他機能・データ・運用など、先に整っている必要がある事項を記載する(この要件自体の制限は §7 制限事項に書く)。
-->
## 6. 前提条件

この要件が成り立つために、先に満たしておく条件を定義する。

| No | 条件 |
|---|---|
| - | なし |

<!--
【7. 制限事項】
定義内容: この要件での制限や範囲外などを定義する。
定義する条件: 制限・範囲外がある場合に定義する。無ければ No 列に「-」、制限列に「なし」を記載する。
項目説明:
- No: 制限の連番(無い場合は「-」)。
- 制限: この要件で設ける制限・範囲外・除外の内容。
定義ルール:
- この要件自体が持つ範囲・除外などを記載する(成立前提は §6 前提条件に書く)。
-->
## 7. 制限事項

この要件での制限や範囲外などを定義する。

| No | 制限 |
|---|---|
| - | なし |
