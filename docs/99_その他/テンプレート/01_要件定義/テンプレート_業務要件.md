<!-- コピーして 01_要件定義/01_業務要件/BR-XXX_要件名.md として使用。index.md への行追加を先に行うこと -->
<!-- 業務要件(BR)は、システムで実現したい業務上の目的・到達状態(To-Be)を定義する。実現手段である機能要件(FR)・共通機能要件(CFR)はトレース先として関連付ける -->
<!-- 各セクション直上のコメントに「定義内容(そのセクションの意味)」「定義する条件」「項目説明(各列・各項目の意味)」「定義ルール」をセットで記載する。編集時はコメントを読んでから該当セクションを埋める -->
<!-- 【フォーマット厳守】各表の列見出し(ヘッダ行)およびセクション構成(##/###)はテンプレート定義のまま変更しない。列の追加・削除・改名、見出しへの補足付記、テンプレートに無いセクション・表・小見出しの追加は禁止。定義済みの表には行の追加と各セルの内容記入のみを行う(docs/CLAUDE.md §8-1・§10-4)。補足はセル内の値、または各セクションの説明文(段落)に記載する -->

<!--
【1. 基本情報】
定義内容: この業務要件の識別情報(要件ID・要件名・概要・実現したい状態・背景)を一覧で示す。
定義する条件: 全 BR で必須。
項目説明:
- 要件ID: この業務要件の識別子(BR-XXX 連番)。
- 要件名: 業務要件の日本語名称。
- 概要: この業務要件の内容(1〜3行)。
- 実現したい状態: この要件が達成されたときに成立している業務上の状態(To-Be)。
- 背景: この要件が必要とされる理由・現状の課題(As-Is)。
定義ルール:
- 要件ID は BR-XXX の連番。採番は index.md の最大値 +1(欠番の再利用は禁止)。
- 業務レベルの目的・状態のみを書く。画面項目・API仕様・DB項目・処理ロジック・実装方式(技術名)は書かない。
- 概要・実現したい状態・背景は簡潔に(各1〜3行)。
-->
## 1. 基本情報

| 項目 | 内容 |
|---|---|
| 要件ID | BR-XXX |
| 要件名 |  |
| 概要 | (1〜3行) |
| 実現したい状態 |  |
| 背景 |  |

<!--
【2. 業務要件】
定義内容: この業務要件で実現したいことを、業務の観点で定義する。
定義する条件: 全 BR で必須。
項目説明:
- No: 項目の連番。
- 実現したいこと: 業務上達成したい事柄・要求。
定義ルール:
- 業務レベルの what(何を実現したいか)のみを書く。実現手段・処理ロジック・実装方式は書かない(手段は §4 関連の FR/CFR で辿る)。
- 1行1件で、達成できたか判断できる粒度で記載する。
-->
## 2. 業務要件

この業務要件で実現したいことを定義する。

| No | 実現したいこと |
|---|---|
| 1 |  |

<!--
【3. 成功指標・期待効果】
定義内容: この業務要件の達成度を測る指標と、達成により得られる業務上の効果を定義する。
定義する条件: 全 BR で必須。指標が数値化できない場合は定性的な期待効果を記載する。
項目説明:
- No: 項目の連番。
- 区分: 成功指標／期待効果。
- 内容: 達成を判断する指標、または達成により得られる業務上の効果。
定義ルール:
- 成功指標はできる限り確認可能な形で記載する。画面・API・DBの実装詳細は書かない。
- 期待効果は業務上のねらい(時間短縮・機会損失防止・公平性向上など)を記載する。
-->
## 3. 成功指標・期待効果

この業務要件の達成度を測る指標と期待効果を定義する。

| No | 区分 | 内容 |
|---|---|---|
| 1 | 成功指標 |  |

<!--
【4. 関連(実現する機能要件)】
定義内容: この業務要件を実現する機能要件(FR)・共通機能要件(CFR)を関連付ける。
定義する条件: 全 BR で必須。
項目説明:
- ID: この業務要件を実現する FR-XXX / CFR-XXX。
- 名称: 当該機能要件の名称(当該文書からの転記)。
- 実現内容: その機能要件が本業務要件のどの部分を実現するか。
定義ルール:
- 実現手段である FR / CFR のみを関連付ける(機能設計 SCR/API/MOD/TBL/JOB は参照しない)。
- 参照先の ID は実在するものだけを記載する。
-->
## 4. 関連(実現する機能要件)

この業務要件を実現する機能要件(FR)・共通機能要件(CFR)を定義する。

| ID | 名称 | 実現内容 |
|---|---|---|
| FR-XXX |  |  |

<!--
【5. 前提・制約】
定義内容: この業務要件が成り立つための前提と、要件に設ける制約・範囲外を定義する。
定義する条件: 前提・制約がある場合に定義する。無ければ No 列に「-」、内容列に「なし」を記載する。
項目説明:
- No: 項目の連番(無い場合は「-」)。
- 区分: 前提／制約。
- 内容: 成立前提、または制約・範囲外の内容。
定義ルール:
- 前提には他業務・データ・運用など先に整っている必要がある事項を、制約にはこの要件の範囲・除外を記載する。
- 実装方式・技術名は書かない(業務レベルの前提・制約のみ)。
-->
## 5. 前提・制約

この業務要件の前提と制約を定義する。

| No | 区分 | 内容 |
|---|---|---|
| - | 前提 | なし |
