# リポジトリ運用ルール

本リポジトリは、要求定義から詳細設計までを0〜12章の一貫した構成で管理する設計書テンプレートと、社員名簿管理システムの記入例を、設計種別ごとに分割した文書として提供する。

## 1. リポジトリの構成

| パス | 役割 |
|---|---|
| `テンプレート/` | 0〜12章を設計種別ごとに分割した**テンプレートの正本** |
| `社員名簿管理システム/` | テンプレートに従って作成した**記入例**。画面モックアップを含む |
| `.github/` | Markdownリンク検査などのCI設定 |

廃止済みの英字ディレクトリ・中間ディレクトリ、および廃止した単一ファイル版（`*_統合設計書.md`）は再作成しない。

## 2. 正本と同期ルール

| No | ルール |
|---:|---|
| 1 | フォーマット、セクション構成、定義ルールを変更する場合は、最初に`テンプレート/`の対応章を更新する |
| 2 | テンプレートの章構成・定義ルールの変更は、同じ作業で`社員名簿管理システム/`の対応章へ反映し、0〜12章の構成を一致させる |
| 3 | 記入例へ適用する変更(実データ・記入サンプル)は`社員名簿管理システム/`の対応章に反映し、テンプレートの空欄プレースホルダ様式を壊さない |
| 4 | テンプレートと記入例の差は、空欄プレースホルダと実データ、およびナビゲーション行・配置階層に応じた相対リンクだけに限定する |
| 5 | 画面モックアップは`社員名簿管理システム/mockups/`でHTML原本とPNGを同じ画面IDで対にして管理する |
| 6 | 章、画面、テーブル、API、JOB、モジュール、SQLの追加・削除・再採番では、README、一覧、トレーサビリティ、レビュー項目、冒頭の目次、相対リンクを同じ作業で更新する |
| 7 | 機能要件では対象範囲内の全F-IDをUCへ対応させ、利用者操作、クライアント共通操作、外部イベント、定期・非同期起動を含む全UCを一覧・個別定義・トレーサビリティで同期する |
| 8 | 機能要件の各UCの基本・代替・例外フローは、内容表（Step・要求・内容）を正本とし、補助シーケンス図を併記する。図の登場者は主アクター(U)・論理的なUI境界(UI)・システム(FUNC)に限定する。UIは、利用者の操作・入力をシステムへ渡し、システムの結果を利用者へ提供する論理的な境界であり、特定の画面・画面遷移・UI部品・クライアント実装を表さない。複数のUI participant、API・モジュール・データベース・実装方式を持ち込まない。番号付きメッセージは内容表と一対一にする。UI participantを持つすべての図は、基本フローへの合流や要求線・共通処理の省略有無にかかわらず、最終結果の`FUNC-->>UI`と利用者提供の`UI-->>U`を明示する。判定結果の戻りは番号なし、利用者への結果提供は`UI-->>U`とする。ALT/EXCは基本フローとの差分だけを示し、先頭に省略範囲、再合流時は末尾の戻りより前に省略する中間処理の範囲を明記する |
| 9 | 画面・API・JOB・モジュールの存在、ID、正式名称、依存方向は§3.1・§5.2・§6.2・§7.2の各一覧を正本とし、各章はそれを変更せず詳細化する |
| 10 | 大セクション(章`#`・主要な設計節`##`)の概要は2行(2文)以内の定型文とする。文1「本〈章/節〉は、【定義対象】を定義する」、文2「関連ルールの正本§X・詳細の委譲先・非対象」。目的・背景の叙述節(§0.1・§0.2・§1.1)は対象外 |
| 11 | 各設計文書本文の「§N」「§N.x(.y)」参照はMarkdownリンク`[§X](0N_ファイル.md#アンカー)`にする(同一ファイルもファイル名付き、アンカーはGitHub式スラッグ)。単一節に解決できないワイルドカード(§N.x等)、HTMLコメント内、本運用文書(CLAUDE.md・README)は対象外 |
| 12 | 各設計文書は、H1見出し(＋概要)の直後に太字ラベル`**目次**`を置き、当該ファイルのH2見出しを1行1項目のリンク`- [見出し全文](#GitHub式アンカー)`で過不足なく列挙する。機能要件（`02_機能要件.md`）だけはH2〜H4の全見出しを階層付きで列挙し、同名見出しのアンカーには出現順の連番を付ける。その他の文書ではH3以下・番号なし見出し・目次自体は載せない。番号なし`##`見出しは作らない。同一ファイル内アンカー(GitHub式スラッグ)とし、テンプレートと記入例で同一様式を保つ。対象見出しの追加・削除・改名・再採番は同じ作業で目次へ反映する |
| 13 | 機能要件のALT/EXCシーケンス図では、図の先頭に無番号のNote「基本フローの〈分岐・発生直前のシーケンス〉までは同一」を置いて、省略した共通前半を明示する。基本フローへ再合流して中間処理を省略する場合は、最終の戻り線より前に「以降、基本フローの〈合流先〉から〈最終内部処理〉までは同一」を置き、省略範囲と最終応答の順序を明示する |
| 14 | 機能要件のALT/EXCで基本フローの特定ステップを差し替える場合は、図と内容表で基本フローと同じ番号・シーケンス名を使用する。差分はサマリ表の条件・フローと内容表の内容欄で明示する |
| 15 | 機能要件で「有効」「無効」「妥当」を用いる場合は、対象の存在・利用状態・基準日・有効開始日・有効終了日・取消状態等を独立した判定軸として各UCの状態定義へ記載し、境界条件を明記する。複数項目の組合せと判定結果は各UCの状態パターンで定義し、有効期間は開始日・終了日を含むか、終了日未設定をどう扱うかを明記する。状態軸は「〜状態」とし、「〜性」を使用しない。複数UCで共通利用する固定区分値は2.5 共通区分定義を正本とする |
| 16 | 機能要件の各UCは、出力データと状態パターンの間に「状態定義」を置き、「項目・意味・内容」の3列表で状態パターンの各状態軸を同名で1行ずつ定義する。内容欄はHTMLの箇条書き（`<ul><li>値：成立条件</li>...</ul>`）とし、「／」で複数の値を連結しない。状態軸の項目名は判定対象が分かる具体名とし、「妥当性」「有効性」「一意性」「整合性」等の「～性」は使用せず該当する軸を「～状態」で表す。状態以外の軸は「～権限」「～区分」「～の有無」等で対象を明示する。独立して判定できる複数の観点を1項目へまとめず、観点ごとに状態軸を分割する。状態パターンに現れるすべての値の意味・成立条件・境界を定義し、「－」は先に成立した条件により当該パターンの判定には使用しないことを表す。パターンID・対応フロー・遷移後状態は状態軸に含めない |
| 17 | 機能要件の状態パターンは、「パターンID」＋状態軸＋「対応フロー」＋「遷移後状態」の列順による完全な決定表とする。状態定義の全値を対応列へ1回以上出現させ、先行軸が正常値の場合に後続軸の各値がどの対応フローと遷移後状態へ到達するかを漏れなく定義する。登録・更新、利用停止・再有効化、存在しない・退職済み等、独立して成立する状態や操作を1セルへまとめず、同じフローへ合流する場合も別パターンとして残す。理論上成立しない組み合わせは行を作らず、状態定義の意味・内容欄で成立しない理由を明記する。先行条件により判定不要となる列だけを「－」とする |
| 18 | 機能要件のシーケンス図で処理ステップの条件・差分を短く補足する場合は、対象participantの側に`Note right of <participant>: <補足>`で置く。当該図のinitで`noteBkgColor`と`noteBorderColor`を`transparent`にし、枠なしのテキストラベルとして表示する。補足は番号を付けず、内容表の独立行にしない |
| 19 | 機能要件テンプレート固有の項目別定義、必須区分、ID・命名規則、完成判定は`テンプレート/02_機能要件.md`内のHTMLコメントを正本とし、独立した機能要件記載ルール文書を作らない。条件付き必須の非該当は対象ブロックを削除せず、`対象外：〈客観的な理由〉`と記載する |
| 20 | 機能要件の権限要件は2.2のH2として2.3 ユースケースより前に配置し、各UC内に個別の権限要件ブロックを設けない。2.3直下のH3は、出現順に`2.3.1`から欠番なく採番し、追加・削除・並べ替え時は2.3直下の全H3を同期して振り直す。節番号は位置を示し、安定識別にはUC-IDを使用する。ユースケース一覧・各UCは番号付きH3とし、ユースケース一覧は見出し直下にMarkdown水平区切り線`---`を置く。各UC間は`---`で区切る |
| 21 | UCで操作が状態パターンの判定条件になる場合は、`**\<操作定義\>**`の操作・説明表で操作を定義する。UCの状態定義に「操作」「操作区分」「要求操作」を状態項目として含めず、状態パターンでは「操作」列と操作時点の各項目状態列を区別する |
| 22 | 対象の存在、項目間の比較、重複その他の判定に必要な状態軸・組合せ・判定結果は、必要とする各UCの状態定義・状態パターン・フローで定義する。複数UCで共通利用する静的な業務区分だけを2.5 共通区分定義へ定義する |
| 23 | 2.5 共通区分定義は、区分ごとに`2.5.1`から欠番なく採番したH3を設け、各見出し直下にMarkdown水平区切り線`---`を置く。各区分は概要2行以内と「区分値・意味合い」の2列表で構成し、区分値が表す業務上の意味を1行1値で定義する。指定可能な機能・操作可否・状態遷移は意味合いへ記載しない |
| 24 | 2.2 権限要件のH3は、`2.2.1 アクター定義`、`2.2.2 権限マトリクス`の順で配置し、各見出しの直下にMarkdown水平区切り線`---`を置く。2.2.1は、マトリクスで使用する全アクターを「アクター・意味合い」の2列表で定義する。意味合いは対象者の利用段階、テナントとの関係、業務上の役割または起動主体を定義し、機能の実行可否は記載しない。2.2.2は、2.1の全F-IDを操作の行、対象範囲内の全アクター／ロールを列とする権限マトリクスで定義する。操作は`F-XXX 機能名`とし、2.1の機能要件を正本として独自の操作名を追加しない。セルは○（実行可）、△（セル内に明記した対象範囲・項目・操作に限り実行可）、－（当該アクター／ロール単独では実行不可）のいずれかとし、権限の正本を2.2へ集約する。権限不足・範囲外要求に対する振る舞いは各UCの状態パターン・例外フローで定義する |
| 25 | 2.3のユースケース一覧は「UC-ID・ユースケース・主アクター・対応機能」の4列で1行1UCとする。主アクターは個別UCと一致させ、起動契機は各個別UCを正本として一覧には記載しない |

## 3. アーキテクチャ規約

- バックエンドはCloudflare Workers Paid、永続化データベースはCloudflare D1（SQLite互換）を標準とする。
- D1 Binding `env.DB`、Prepared Statement、SQLを扱えるのはM-006 D1データアクセスだけとする。
- APIはログイン以外をM-002、ログインをM-003へ委譲し、JOBはM-002へ委譲する。API・JOBからM-006やD1を直接呼ばない。
- API、JOB、モジュールの各処理は、番号と名称が一致する「処理フロー」と「処理詳細」で記載する。
- M-006が実行するSQLはクエリ設計にSQL-ID付きで定義し、SQLite/D1構文と順序付きplaceholderを使用する。

## 4. READMEの責務

| 階層 | READMEの役割 |
|---|---|
| ルート | リポジトリの目的、メイン文書、利用方法、技術前提 |
| `テンプレート/` | 分割テンプレートの章一覧と利用方法 |
| `社員名簿管理システム/` | 記入例の章一覧、システム前提、テンプレートへの導線 |

## 5. CI

[.github/workflows/markdown-link-check.yml](.github/workflows/markdown-link-check.yml)で、ルートREADMEとCLAUDE.md、テンプレート・社員名簿管理システムの各分割版のMarkdownリンク切れを検査する。移動・改名時は旧パスへの参照を残さない。
